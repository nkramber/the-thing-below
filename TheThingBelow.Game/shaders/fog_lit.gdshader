// The fog of a weather that takes the scene light of the map, as a figure does (D-183, D-897).
// `fog_noise.gdshaderinc` holds the fog.
shader_type canvas_item;

#include "res://shaders/fog_noise.gdshaderinc"
