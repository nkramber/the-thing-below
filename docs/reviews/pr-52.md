# PR-52 review

Date: 2026-09-21

## Identity

- PR: 52
- Target: `main`
- Base: `9e9dc59`
- Merge base: `9e9dc59`
- Head: `2f41276`
- Branch: `feat/pr-48-normal-maps`

## Provider gate

The handoff entry for PR #52 identifies Claude Code as the author provider. The active reviewer is Codex, so the opposite-provider gate passes under T-4 and D-17.

## Intended behavior and scope

The PR adds integer normal-map generation, palette heights, override-grid validation, normal-map atlas pages, review sheets, and the capture input regression fix. The complete merge-base diff covers 41 paths. I inspected every changed path, including code, tests, content images, identity data, skills, roadmaps, decisions, and handoff files. No area remains uninspected.

## Findings

No finding.

## Out of scope

The roadmap assigns scene-light consumption of the normal maps to PR-56 and later effect work. This review does not require that later Game behavior.

## PR comments

- Gitar reported no code issues, with functional validation disabled. The review verified the claim against the complete diff and the local contract-specific checks. Its separate review-gate configuration fault only reported that this review record was absent.

## Description edits

None.

## Verification

- `git fetch origin`: passed. The local branch matched the PR implementation head `2f41276` before this metadata commit.
- `make verify` at `2f41276`: passed. Build, 1,839 tests, format, det-lint, STE check, replay identity, content hash, atlas and normal-map pixel checks, and smoke passed.
- Focused contract checks: passed. The test suite covers integer-root boundaries, normal-map slopes, palette heights, override directions and shape errors, stale normal pages, alpha coverage, atlas regeneration, and the capture input regression.
- GitHub checks at `2f41276`: all implementation checks and Gitar passed. `review-gate` failed only because `docs/reviews/pr-52.md` did not yet exist. The failure log confirms RG 6 to RG 8 pass.
- Screen evidence: the PR records the author's read of all four normal-map sheets and states that the PR changes no screen. The local smoke and atlas checks passed.
- Push: `1c3236a` is the head of origin/feat/pr-48-normal-maps, verified with gh pr view.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** This verdict applies to head `2f41276`. The complete diff has review coverage, the opposite-provider gate passes, no finding remains, and all required current checks pass except the review gate that this record publishes.
