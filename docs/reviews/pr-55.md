# PR-55 review

Date: 2026-09-22

## Identity

- PR: 55
- Target: `main`
- Base: `daeccfe6715e15d3b085f8d9c70c12b42f082101`
- Merge base: `daeccfe6715e15d3b085f8d9c70c12b42f082101`
- Head: `d6b3606b9e74d2fd2a29972824cd83fc8a51747d`
- Branch: `feat/pr-63-settings`

## Provider gate

The PR handoff entries identify Claude Code as the author provider. The active reviewer is Codex. The providers differ, so the gate passes T-4 and D-17.

## Intended behavior and scope

The PR adds persisted settings, input remapping, dead-zone control, accessibility settings, runtime application, the settings screen, tests, and screen baselines. The review traced Storage validation and safe writes, Game startup and close paths, input-map rebuilds, remap capture, dead-zone boundaries, settings rows, affected battle callers, and the screen-test fixtures.

Inspected paths from the complete diff: `.claude/skills/csharp-conventions/SKILL.md`; `TheThingBelow.Game/scripts/Boot.cs`, `CaptureSession.cs`, `GameRun.cs`, `ScreenCaptures.cs`, `Ui/BattleCommands.cs`, `Ui/BattleScreen.cs`, `Ui/BattleTimes.cs`, `Ui/CommandMemory.cs`, `Ui/CommandMemory.cs.uid`, `Ui/FrameRoot.cs`, `Ui/GameInputMap.cs`, `Ui/ScreenFit.cs`, `Ui/SettingsMenu.cs`, `Ui/SettingsMenu.cs.uid`, `Ui/SettingsScreen.cs`, `Ui/SettingsScreen.cs.uid`, and `Ui/UiBase.cs`; `TheThingBelow.Storage/ControlBindings.cs`, `GameSettings.cs`, `SettingsFormat.cs`, `SettingsStore.cs`, and `SettingsText.cs`; the listed changed test files under `TheThingBelow.Tests`, including `settings/format-1.json`; `content/strings/en.json`; the listed changed decision, design, question, roadmap, handoff, and archive files; and the eight listed screen baselines. No affected area remains uninspected.

Affected contracts are Storage error context and format validation, input and UI boundaries, presentation at the required frame sizes, player strings, and test and screen evidence. Core determinism and replay behavior do not change because no Core file changes.

## Findings

No finding.

## Out of scope

Later PRs consume the saved audio, effect, vibration, and text-speed settings as named in the roadmap and PR description. This review verifies that this PR stores and preserves those values. Their later runtime consumers are out of scope.

## PR comments

- Gitar reported a review-gate tooling failure because the review record did not yet exist, and its code review was approved with no finding. The current head and the local gate evidence verify that the failure is the expected pre-review state.

## Description edits

None.

## Verification

- `git fetch origin`: passed. The local branch matched `origin/feat/pr-63-settings` before the review record commit.
- PR metadata and comment export for PR #55: passed. The author is Claude Code, the current head is `e18b057`, and the effective implementation head is `d6b3606`.
- `make verify` at effective head `d6b3606`: passed. Build, 1,987 non-Smoke tests, format, det-lint, STE, replay identity, content hash, atlas, and headless smoke all passed.
- Focused source and test review: passed. The settings file rejects absent, unknown, repeated, invalid, and conflicting data with contextual errors. Remap capture, dead-zone limits, close refusal, runtime application, and affected callers have coverage.
- CI run 35689788015: passed for build/test/format, det-lint, replay identity, smoke, screen-test, coverage, and changed paths.
- Gitar pass at `e18b057`: approved with no finding.
- Push: `75da593` is the head of `origin/feat/pr-63-settings`, verified with gh pr view.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** This verdict applies to head `d6b3606b9e74d2fd2a29972824cd83fc8a51747d`. The provider gate, complete diff review, focused checks, local repository gate, and current CI evidence pass, with no blocking finding.
