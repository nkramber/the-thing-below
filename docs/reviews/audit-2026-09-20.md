# Audit of the whole repository, 2026-09-20

Auditor: Claude Code
Base: `main` at `4a472c5`. The fixes are on the branch of the audit fixes PR (D-696).
Scope: every source file, test, configuration file, workflow, content file, and document.

This file is a dated record. It uses local ids, A-1 to A-28, and they never enter the design
register. F-93 of `docs/design.md` is the finding of the design register for this audit.

## Method

- Five parallel reads: Core, Tools, Game with Storage, the documents, and the content with the
  repository files. The auditor then checked each claim against the code.
- The hash functions and the generator were derived again from reference values.
- The local checks were green at the base: build, 837 tests, format, det-lint, STE, replay
  identity, and content hash.
- The owner answered A-1 on 2026-09-20: the license change was intended (D-695).

## Findings and their state in the audit fixes PR

| Id | Severity | Finding | Where | State |
|---|---|---|---|---|
| A-1 | Critical | `LICENSE` is GPL-3.0, against D-54, from the direct commit `6910017` | `LICENSE`, `docs/decisions.md` | Owner: intended. D-695 supersedes D-54. The export copies `LICENSE` |
| A-2 | High | The Tools scan of det-lint drops a compile error outside a D-502 folder, so a scan that read nothing passes | `DetLintCommand.cs`, `SourceScan.cs` | Fixed. `DL 0` stays in the report. Regression test in `DetLintCommandTests` |
| A-3 | High | Game records a tick after its step, so the record of a crash omits the tick that crashed | `GameRun.cs`, `Boot.cs` | Fixed. The run records before the step. Regression test in `GameRunTests` |
| A-4 | High | Branch protection does not bind an administrator. One direct commit, and PR #15 and PR #17 with no record and no label | GitHub settings | Closed. The owner enabled the protection of the administrators (D-706, OQ-200). F-93 records the three cases |
| A-5 | Medium | The smoke session writes a check file into the real crash folder and evicts real crash files | `Boot.cs` | Fixed. The check writes into a `smoke` folder inside the crash folder |
| A-6 | Medium | Every JSON reader of Core takes a repeated field and keeps the last value | `ContentReader.cs` | Fixed. Tests for the reader, the record, the save, and the log line |
| A-7 | Medium | A lone surrogate escape or a byte that is not UTF-8 leaves as a bare `InvalidOperationException` | `ContentReader.cs` | Fixed. The reader names the file and the field. Two tests |
| A-8 | Medium | `content-hash` ends with a stack trace on a hash file with no hash line | `ContentHashCommand.cs` | Fixed. Regression test |
| A-9 | Medium | The skip set of CI skips the tests that keep the Documents rows in one list | `ci.yml`, `ReviewGateRuleTests.cs` | Fixed. ste-check rule DOCS 1 runs on every PR |
| A-10 | Medium | The path rule of ste-check knows no `TheThingBelow.Storage`, `.githooks`, or `licenses` | `ReferenceRules.cs` | Fixed. A test reads the solution file |
| A-11 | Medium | No test pins `pipefail` in the CI smoke step, and the comment of the step is wrong | `SmokeExitCodeTests.cs`, `ci.yml` | Fixed. A theory pins both workflow steps |
| A-12 | Medium | The Smoke filter excludes nothing, and no job runs that category | `Makefile`, `ci.yml` | Fixed in part. `TestCategories` holds the option, and a test pins each command. No smoke test exists yet |
| A-13 | Medium | The state of Gate 1 and of the required checks reads three ways | `questions.md`, `phase-1-foundations.md` | Fixed. The done mark of D-681 is in, and D-697 records the D-685 step as done (OQ-201) |
| A-14 | Medium | D-625 stands before D-624, and two rows say "Revised in process by" | `docs/decisions.md` | Fixed |
| A-15 | Medium | `CLAUDE.md` says that a Python atlas script exists | `CLAUDE.md`, `AGENTS.md` | Fixed |
| A-16 | Medium | Session 117 sits at the end of the archive under a level-one heading, and the archive holds a second title | `docs/session-handoff-archive.md` | Fixed. ste-check rule HANDOFF 4 refuses such a heading. Sessions 86, 94, 95, 97, and 119 exist in no file |
| A-17 | Medium | `RunRecorder.Step` takes a tick gap | `RunRecorder.cs` | Fixed. Test |
| A-18 | Medium | `LogLineText.Read` lets a constructor error leave with no line context | `LogLineText.cs` | Fixed. Three cases |
| A-19 | Medium | A dropped tick writes no log entry | `FixedStepLoop.cs`, `GameRun.cs` | Fixed. The run adds an error entry with the count. Test |
| A-20 | Medium | Test gaps: the SHA-256 block edges, the state hash layout, and the cross-file rules of the content set | Tests | Fixed. Ten digests, a layout oracle, and twelve negative tests |
| A-21 | Medium | RG 7 reads the form of a Documents line and not its truth. The review record is in the metadata set | `DocumentRules.cs`, `EffectiveHead.cs` | RG 7 reads each line against the diff. The `one-pr-one-session` skill states the trust boundary of the record |
| A-22 | Medium | det-lint gaps: interface walks, named property calls, menu item calls, item text in a scene, threads, conversions, two sort shapes | `DetLint/` | Fixed. DL 10 is new. Tests for each shape |
| A-23 | Medium | A page file that no index names passes the load, and no check runs `atlas --check` | `ContentSet.cs`, `Makefile` | Fixed. The load refuses the page, and `make verify` runs `make atlas` |
| A-24 | Low | Storage and Game details | `Boot.cs`, `FolderFiles.cs`, `LogStore.cs`, `SaveFolder.cs`, `FixedStepLoop.cs` | Fixed: the crash catch, the dead catch, the log remark, the file that a cleanup keeps, the temporary file of a torn write, NaN, the F-50 citation, the environment error, the file fault filter, the second content load, the build callback guard |
| A-25 | Low | Core details | `SimulationVersion.cs`, `ContentSet.cs`, `RunSnapshot.cs`, `AtlasIndex.cs` | Fixed: the version remark, the repeated path, the use list of the index, the exact increment, one line splitter, the reader message, the use and the ticks of the index. The hasher has a remark on tags |
| A-26 | Low | Tools details | `PngReader.cs`, `EnglishWords.cs`, `IdRegister.cs`, `AtlasCommand.cs`, `ReviewSheet.cs`, `SwatchSheet.cs` | Fixed: the zlib check value, the dead word entries, the second supersession form, the first different pixel, more sheets for a large batch (D-668), the swatch cell width, the build symbols, one option parser, the list item pattern |
| A-27 | Low | Documents behind the code | Four skills, two runbooks, the PR template, `design.md`, `questions.md` | Fixed. D-701, D-703, D-704, and D-705 answer the four points of OQ-205, OQ-207, OQ-208, and OQ-209 |
| A-28 | Low | Repository files | `.gitignore`, `.gitattributes`, `global.json`, `.editorconfig`, `ci.yml` | Fixed: `spike/` is ignored, three binary types, the SDK rolls to the latest patch alone, an editor settings file for the text rules, the merge base diff of the changed paths job, and no cancel of a run on `main` |

## The questions of the audit and their owner answers

The audit raised OQ-200 to OQ-209. The owner answered each one on 2026-09-20, and three answers asked for code.

| Question | Answer | State in the audit fixes PR |
|---|---|---|
| OQ-202, the kind of a cast id | D-698: `cast` | Fixed. Two code comments took the kind |
| OQ-203, the export trigger paths | D-699: the Game project file joins the set | Fixed. `export.yml` holds five paths. `ExportWorkflowTests` pins the list, and it failed on the four paths |
| OQ-204, the harness settings file | D-700: `.claude/settings.json` takes the review | Fixed. `OverrideRules.IsEligible` refuses that one path, and RG 1 gives it a reason of its own. Two regression tests in `ReviewGateOverrideRuleTests` |
| OQ-206, the files of the STE check | D-702: the tracked files alone | Fixed. `DocumentSet` reads `git ls-files -z` when the root holds `.git`, and a `git` run that fails is an error. Regression tests in `SteCheckTrackedFileTests` |

## Not changed, with the reason

- `TheThingBelow.Game.csproj` keeps `NoWarn CS1591`. The Godot source generators write members that no comment can cover, and no narrower switch exists.
- `SaveStore.Read` removes the resume file at the read. D-258 sets that rule.
- `SafeWrite` syncs the file and not the folder. The old file stays whole after a power loss, which is the promise of D-178.
- `docs/reviews/pr-19-response.md` holds a wrong repository path. The line is itself the correction of that path in a dated record.
- A `PackedStringArray` of item text in a scene file takes no DL 9 rule. Godot 4 writes item text as `item_N/text`, which the rule reads now.
- The five squash subjects with no prefix stay, because history does not change.

## Evidence of the fixed state

On the branch, on the Mac of the owner, `make verify` passes: the build, 953 tests, the format check, det-lint with 0 findings, the STE check with 0 findings, the replay identity on version 4, the content hash, the atlas check, and the smoke session. The STE check passes with two untracked notes in the checkout (D-702). The three CI legs are the checks of the PR.
