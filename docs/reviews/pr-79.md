# PR-79 review

Date: 2026-09-25

## Identity

- PR: 79
- Target: `main`
- Base: `5b42cf0d5e0b35019ffd1a6e5aea60cad64b4bb1`
- Merge base: `5b42cf0d5e0b35019ffd1a6e5aea60cad64b4bb1`
- Head: `950118384c46297fe5ff52f0e805311d97b0e1eb`
- Branch: `fix/pr-102-gate-hygiene`

## Provider gate

The author provider is Claude Code, as the author handoff entries for PR #79 state. The reviewer provider is Codex. They differ, so the review passes T-4 and D-17.

## Intended behavior and scope

The PR fixes seven repository gate findings, records four owner answers, and updates the PR-102 roadmap. The review inspected all 29 paths in the diff from merge base `5b42cf0` to effective head `9501183`. The merge commit includes PR-101; its changed behavior and inherited files were also inspected. No path remains uninspected.

Paths inspected: `.claude/skills/pr-review/references/review-record.md`, `.claude/skills/ste-writing/SKILL.md`, `.github/pull_request_template.md`, `.github/workflows/ci.yml`, `.github/workflows/export.yml`, `.github/workflows/review-gate.yml`, `.gitignore`, `AGENTS.md`, `CLAUDE.md`, `TheThingBelow.Tests/CodexReviewExternalProgramTests.cs`, `TheThingBelow.Tests/ExportWorkflowTests.cs`, `TheThingBelow.Tests/ReviewGateRuleTests.cs`, `TheThingBelow.Tests/SteCheckAgentFileTests.cs`, `TheThingBelow.Tests/SteCheckDocumentRowTests.cs`, `TheThingBelow.Tests/WorkflowPathListTests.cs`, `TheThingBelow.Tools/CodexReview/CodexReviewCommand.cs`, `TheThingBelow.Tools/CodexReview/ExternalProgram.cs`, `TheThingBelow.Tools/ReviewGate/OverrideRules.cs`, `TheThingBelow.Tools/SteCheck/AgentFileRules.cs`, `TheThingBelow.Tools/SteCheck/DocumentRowRules.cs`, `TheThingBelow.Tools/SteCheck/DocumentSet.cs`, `docs/decisions.md`, `docs/design.md`, `docs/roadmaps/area-ci.md`, `docs/roadmaps/area-tools.md`, `docs/roadmaps/phase-2-first-playable.md`, `docs/runbooks/merge.md`, `docs/session-handoff-archive.md`, and `docs/session-handoff.md`.

Affected contracts include workflow path classification and concurrency, visible export errors, the review override boundary, exact STE checks, bounded external processes and cleanup, PR description and handoff requirements, and document history. The owner approved the multi-finding scope for this PR in D-1081. The review checked the exit tests in section 7.38 of `docs/roadmaps/phase-2-first-playable.md` and the focused CI roadmap in section 7.24 of `docs/roadmaps/area-ci.md`.

## Findings

No finding.

## Out of scope

None.

## PR comments

- The CI analysis for runs `36092848554` and `36093261322` claimed a `review-gate` fault. The author answered that RG 3 alone failed because this record was not yet present. The CI logs show that expected condition; this record supplies it.
- The Gitar review thread reported that case variants of the local settings path could pass the label rule on case-insensitive systems. The author fixed the comparison and added case-variant tests at `74b8020`. The reviewed code uses `OrdinalIgnoreCase`, and the focused tests pass.
- The CI analysis for run `36096757222` again claimed a `review-gate` fault. The author answered that RG 3 alone failed because this record was not yet present. The run reports the expected pre-publication state; the metadata push will trigger a fresh check.
- The clean approval and status text in the Gitar comment contain no separate item, so D-964 requires no answer.

## Description edits

None.

## Verification

- `git diff --stat 5b42cf0..9501183`: 29 paths inspected; no path omitted.
- `make verify` at `9501183` on macOS arm64: build passed with 0 warnings; 3,256 tests passed; format passed; det-lint, ste-check, replay identity, content hash, atlas, and smoke passed.
- `WorkflowPathListTests`, `ExportWorkflowTests`, `ReviewGateRuleTests`, `SteCheckAgentFileTests`, `SteCheckDocumentRowTests`, and `CodexReviewExternalProgramTests` ran as part of the 3,256 passing tests.
- `gh pr checks 79` at `9501183`: build/test/format passed on all three legs; changed paths, coverage, det-lint, export, replay identity, screen-test, smoke, and STE checks passed. `Gitar` passed. `review-gate` failed at RG 3 because this review record was absent before publication. Bot and night-gate checks do not exist yet; PR-15 and PR-49 create them.
- The review-gate workflow reads the PR head as data and does not execute it. Inspection confirmed the large values go to jq through files, path lists disable rename detection, and the commit loop preserves pipeline failures under `set -euo pipefail`.
- The timeout implementation bounds process execution and output draining, kills the process tree at timeout, and reports an output pipe that remains open. The regression tests cover timeout, child cleanup, and a child that retains output after normal exit.
- Push: the metadata commit containing this record is the head of `origin/fix/pr-102-gate-hygiene`, verified with `gh pr view` after publication.

## Open questions and accepted risks

No open question blocks this PR. The bot and night-gate checks remain future checks created by PR-15 and PR-49. This PR changes no screen, so no visual review applies.

## Verdict

**Ready for owner merge.** This verdict applies to head `950118384c46297fe5ff52f0e805311d97b0e1eb`. The provider gate, full diff review, focused exit tests, and required current CI evidence pass. The sole CI failure is RG 3 before this record is published; a fresh review-gate result must read the pushed record.
