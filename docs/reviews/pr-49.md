# PR-49 review

Date: 2026-09-21

## Identity

- PR: 49
- Target: `main`
- Base: `74c3a64`
- Merge base: `74c3a64`
- Head: `34e6272`
- Branch: `feat/pr-66-elements-statuses`

## Provider gate

The handoff entries for PR #49 identify Claude Code as the author provider. The active reviewer is Codex. The providers are different, so the cross-provider gate passes under T-4 and D-17.

## Intended behavior and scope

PR #49 adds elemental damage, ten battle statuses, status persistence, save format 5, and the related replay identity data. The review inspected the Core battle state, turn loop, damage and status rules, content readers, save and snapshot readers, identity runs, tests, changed documentation, and the response to the Gitar finding.

Inspected paths from the complete diff:

- `.claude/skills/ste-writing/references/glossary.md`
- `TheThingBelow.Core/Battles/` changed files
- `TheThingBelow.Core/Identity/IdentitySet.cs`
- `TheThingBelow.Core/Runs/RunSnapshotText.cs`
- `TheThingBelow.Core/Saves/` changed files
- `TheThingBelow.Core/SimulationVersion.cs`
- `TheThingBelow.Tests/` changed files
- `content/rules/` changed files
- `docs/decisions.md`
- `docs/design.md`
- `docs/roadmaps/` changed files
- `docs/session-handoff.md`
- `docs/session-handoff-archive.md`

The Core boundary, determinism, replay, errors, content, gameplay, and save compatibility contracts apply. Presentation and dependency changes do not apply. No changed path remains uninspected.

## Findings

No finding.

## Out of scope

The player move path and map behavior for persistent statuses belong to PR-12 and PR-64 as recorded in the roadmap. No severity applies.

## PR comments

- Gitar reported that stunning the character whose turn was open could run start-of-turn shares twice. The author answered with `34e6272`. The review verified the new error path, the unchanged state assertion in `AStunOnTheCharacterWhoseTurnIsOpenIsAnErrorAndChangesNothing`, and the current Gitar verification. The finding is fixed in `34e6272`.

## Description edits

None.

## Verification

- `git fetch origin`: passed. The merge base is `74c3a64`, and the effective implementation head is `34e6272`. Commit `3690b61` changes metadata only.
- `git diff --stat 74c3a64...3690b61`: passed. All 37 changed paths were inspected.
- `make verify`: passed. Build, 1,625 tests, format, det-lint, STE, replay identity, content hash, atlas, and smoke passed on the local macOS arm64 machine.
- `gh pr checks 49`: passed for the reviewed head for build, test, format, det-lint, replay identity, smoke, screen-test, STE, coverage, changed paths, and Gitar. The review-gate check waits for this record.
- The Gitar response at `34e6272` is current and verifies the correction.
- Push: pending. The review record and handoff entry must be committed and pushed before this line can be verified.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** This verdict applies to head `34e6272`. The reviewed behavior and required checks pass, and the earlier Gitar finding is fixed.
