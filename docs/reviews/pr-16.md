# PR-16 review

Date: 2026-09-16

## Identity

- PR: 16
- Target: `main`
- Base: `2bc7d56728baff7d6b6b4e0dbf89aee087511f41`
- Merge base: `2bc7d56728baff7d6b6b4e0dbf89aee087511f41`
- Head: `d2479cee93bc02dee15f6c4397054aa152fe7b11`
- Branch: `docs/pr-16-context-budget`

## Provider gate

The author provider is Claude Code. Session 50 of `docs/session-handoff.md` records the author role for PR #16. The active reviewer is Codex. The providers differ, so the gate passes under T-4 and D-17.

## Intended behavior and scope

The PR reduces repeated context reads and checks while preserving review, writing, and handoff gates. The review inspected all 20 paths in the merge-base diff: `.claude/agents/design-critic.md`, `.claude/skills/one-pr-one-session/SKILL.md`, `.claude/skills/pr-review/SKILL.md`, the five `.claude/skills/pr-review/references/*.md` files, `.claude/skills/ste-writing/SKILL.md`, `.claude/skills/ste-writing/references/glossary.md`, `AGENTS.md`, `CLAUDE.md`, `docs/decisions.md`, `docs/design.md`, `docs/questions.md`, `docs/reviews/pr-16-response.md`, `docs/reviews/pr-16.md`, `docs/runbooks/session-context.md`, `docs/session-handoff-archive.md`, and `docs/session-handoff.md`. No changed path remains uninspected. Since the earlier review, the runbook correction and its response were inspected in full, and the session handoff rotation was checked for continuity.

The affected contracts are session binding and handoff (D-18, D-576 to D-587), review evidence and provider eligibility (T-4, D-17, D-588, D-589), STE and glossary use (D-10, D-12, D-590), and script handling (D-591). The decision effects, finding F-59, question OQ-182, and design register align with the described change. The archived Session 40 entry matches its former handoff text. The project glossary content moved word for word; three process terms remain in the process glossary as D-590 requires. `AGENTS.md` and `CLAUDE.md` are identical.

## Findings

### P2-1: The PR comment export can succeed with missing comments

Status: fixed in `d2479ce`.

File: `docs/runbooks/session-context.md:95-102`.

Trigger: Any earlier `gh pr view` or `gh api` call in the grouped command fails, and a later call succeeds.

Expected: The command must fail when any source of PR comments or review data fails, so the reviewer cannot mistake a partial file for the complete evidence required by D-589 and T-2.

Actual: The command uses a brace group without `set -e` or `&&`. The exit status comes from the final call. A shell reproduction with a failing command followed by a successful command returned status 0 and wrote a partial file.

Consequence: A reviewer can miss a Gitar claim, author reply, or review comment while believing the required comment export completed.

Correction: Make every retrieval failure stop the command before it leaves a usable comments file. For example, use `set -e` and write to a temporary file that is renamed only after all calls succeed.

Regression check: `/tmp/pr16_export_regression.sh` ran on the prior and corrected runbook text in bash and zsh. The prior text returned 0 and left `comments.md` in both shells. The corrected text returned 1 and left no `comments.md` in both shells. This establishes the fix for the original trigger.

### P2-2: The commit command stops when no STE files are staged

Status: open.

File: `docs/runbooks/session-context.md:59-63`.

Trigger: Stage only Markdown files under `docs/reviews/` and `docs/session-handoff*`, as a review session does when it commits its required review and handoff records.

Expected: The command must treat an empty list of STE-eligible files as no checker work, then run `git diff --cached --check` and the commit (D-585).

Actual: `grep -v` returns 1 when every staged Markdown file matches an excluded path. Because the assignment is followed by `&&`, the command stops before the empty-list guard and commit.

Consequence: The documented command cannot commit the review and handoff records that the review procedure requires.

Correction: Preserve an empty file list when the path filter has no matches, while keeping checker failures fatal.

Regression check: Stage only the review record and session handoff, run the documented command, and verify that the checker is skipped while the diff check and commit complete successfully.

## Out of scope

None.

## PR comments

- The earlier review recorded Gitar approval and no open threads on `45e960d`. The current export contains a manual request at `2026-09-16T21:41:11Z`, its `On it` reply at `21:41:32Z`, and an Approved dashboard comment updated at `21:41:52Z`. It reports 0 findings and 0 threads for `d2479ce`. The later metadata push moved the branch head to `60260ef`, so this Gitar review is stale under the freshness rule. No formal review or inline review comment exists.

## Description edits

- Corrected the Documents line from review head `45e960d` to `d2479ce`.

## Verification

- `gh pr view 16`: reports base `2bc7d56728baff7d6b6b4e0dbf89aee087511f41` and effective head `d2479cee93bc02dee15f6c4397054aa152fe7b11`.
- `git diff --stat origin/main...d2479ce`: inspected; 20 paths, 1,097 insertions, 646 deletions.
- `git diff --check origin/main...d2479ce`: passed.
- `cmp -s AGENTS.md CLAUDE.md`: passed; files are identical.
- Full interim STE check: passed with 0 findings.
- `bash /tmp/pr16_export_regression.sh`: passed; old and corrected runbook behavior matched the expected exit status and file-presence results in bash and zsh.
- The documented staged-file pipeline with only review and handoff Markdown: failed before the empty-list guard; `grep -v` returned 1 and the `&&` chain did not commit.
- Current PR comment export: passed, saved, and read. Gitar approved `d2479ce`, with 0 findings and 0 threads.
- Gitar freshness: the dashboard update at `21:41:52Z` follows the request reply at `21:41:32Z`, but predates branch head `60260ef`; the review is stale for the current PR head.
- `gh pr checks 16`: no checks reported. The check-runs endpoint for `d2479ce` returned 0 check runs.
- Build, test, format, and `make verify`: not run because the repository has no solution, code, or Makefile.
- `make where`: not available because the repository has no Makefile.
- `gh pr edit 16` and follow-up `gh pr view`: passed; the stale review-head fact now names `d2479ce`.
- `git push origin docs/pr-16-context-budget`: passed. Fetch, clean status, and `gh pr view` verified metadata head `60260ef6304a08c86e1f0cd6a48db6dc1e702180`.
- Push: `60260ef6304a08c86e1f0cd6a48db6dc1e702180` is the head of `origin/docs/pr-16-context-budget`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-182 asks where to place the proposed context-size check and blocks nothing. D-590 accepts that the STE checker does not read the glossary reference file.

## Earlier verdicts

- **Changes required** for head `45e960dbe12470f3ecce3d0015efc1b0b8ac8cd7`. P2-1 identified the fail-open PR comment export. This finding is fixed in `d2479ce`.

## Verdict

**Blocked.** This verdict applies to head `d2479cee93bc02dee15f6c4397054aa152fe7b11`. P2-1 is fixed, and P2-2 remains open because the documented commit command stops on an empty eligible-file list. The latest Gitar approval also predates the current PR branch head, and GitHub reports no check runs.
