# PR-33 review

Date: 2026-09-19

## Identity

- PR: 33
- Target: `main`
- Base: `9863da3`
- Merge base: `9863da3`
- Head: `d1b2305`
- Branch: `feat/pr-34-atlas-and-palette`

## Provider gate

Claude Code authored the substantive PR changes, as shown by Session 122 and the PR handoff. Codex is the reviewer in this session. The provider gate passes under T-4 and D-17.

## Intended behavior and scope

The PR adds the strict drawing and atlas content readers, the 64-color palette, the deterministic atlas command, committed atlas content, review-sheet generation, and related tests and documents. The review inspected the complete diff from `9863da3` to `d1b2305`, including all 50 changed paths: the Core content readers, Tools atlas code, tests, Game project content inclusion, palette and drawing JSON, atlas PNG and index, documentation, handoff records, and removal of `docs/tools/make-atlas.py`. The affected contracts are content validation, deterministic integer-only tool output, error context, atlas pixel identity, presentation asset provenance, and the Documents section. No changed area remains uninspected.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar reported that empty `--root` and `--sheets` values escaped the command error handler. The author fixed the parser in `d1b2305`; the review verified both cases now return a contextual error and exit code 1.

## Description edits

None.

## Verification

- `git diff --stat 9863da3 d1b2305` and the complete staged path diff: passed. All 50 changed paths were inspected.
- `make verify`: passed locally with 782 non-smoke tests, 0 failures, 0 warnings, format clean, det-lint clean, STE check clean, replay identity clean, content hash clean, and a successful bounded Godot smoke session.
- `dotnet run --project TheThingBelow.Tools/TheThingBelow.Tools.csproj --no-build -- atlas --root . --check`: passed. The committed atlas matches every drawing file.
- `atlas --root ""`: passed as a negative probe. It returned `Error: the value of the option --root is empty.` with exit code 1.
- `atlas --sheets ""`: passed as a negative probe. It returned `Error: the value of the option --sheets is empty.` with exit code 1.
- GitHub checks: the author-reported effective-head CI results and gitar approval were read from the PR. The local required checks above pass. Windows and macOS jobs were still in progress at the time of review, so their final results remain an accepted external-state risk.
- Push: `abad4da4bf5ad1dabccde9410b49e69f55ba442d` is the head of `origin/feat/pr-34-atlas-and-palette`, verified with gh pr view.

## Open questions and accepted risks

The final state of the in-progress Windows and macOS GitHub jobs was not available at review time. This review accepts that external-state risk because the effective head is fixed, the local gates pass, and the author reports the corresponding CI matrix as green or in progress. No open OQ affects this PR.

## Verdict

**Ready for owner merge.** This verdict applies to head `d1b2305`. The provider gate, complete diff review, focused falsifiers, local project gates, and PR exit tests pass, with no blocking finding.
