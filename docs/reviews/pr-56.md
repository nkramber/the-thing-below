# PR-56 review

Date: 2026-09-22

## Identity

- PR: 56
- Target: `main`
- Base: `8aaf0f6`
- Merge base: `8aaf0f6`
- Head: `f1b42b5`
- Branch: `feat/pr-57-effects`

## Provider gate

The PR handoff entries identify Claude Code as the author provider. The active reviewer provider is Codex. The provider gate passes T-4 and D-17.

## Intended behavior and scope

The PR adds strict effect files, hit particles, battle timing data, heavy-blow hit-stop, screen shake, effect-level settings, and their tests and screen baselines. The review traced Core loading and cross-file checks, the Game particle and battle-screen path, the settings update path, the capture fixtures, and the affected tests and documents.

Inspected every path in the complete diff from `8aaf0f6` to `f1b42b5`: `.claude/skills/ste-writing/references/glossary.md`, `TheThingBelow.Core/Content/ContentSet.cs`, all four files in `TheThingBelow.Core/Effects/`, `TheThingBelow.Core/Light/EffectBudget.cs`, all affected Game scripts, `TheThingBelow.Storage/GameSettings.cs`, all affected tests, the three effect content files, the affected design, decision, question, roadmap, and handoff files, and all seven screen baselines. No area remains uninspected.

The review applied the Core boundary, determinism, errors, content, presentation, and dependency contracts. The implementation keeps effect timing outside Core rules, validates effect data with file and field context, checks the particle budget, uses fixed seeds for screen seeks, keeps the UI outside the shake target, and provides full, reduced, and off shake forms.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar reported the absent review record as the only review-gate failure and approved the code review. The review verified that the record was absent before this file and that all other current checks pass. No open code-review thread exists.

## Description edits

None.

## Verification

- `make verify`: passed. Build, 2,069 non-Smoke tests, format, det-lint, STE, replay identity, content hash, atlas, and Godot smoke passed.
- `gh pr checks 56`: all current build, test, format, coverage, det-lint, replay identity, smoke, screen-test, STE, review-gate, and Gitar checks passed after the review record push. Docs-only jobs that were not needed skipped.
- `git diff --stat 8aaf0f6..f1b42b5`: inspected all 46 changed paths.
- Screen baseline inspection: all seven PNG files are 1280 by 720 RGBA images. The PR records the required frame review, and the screen-test check passes.
- `dotnet run --project TheThingBelow.Tools/TheThingBelow.Tools.csproj -- review-gate`: not run against the PR body because the local command requires the PR body and head-files inputs. The live review-gate check was read above and failed only because this record was absent.
- Push: `1cddb48` is the head of `origin/feat/pr-57-effects`, verified with `gh pr view`. The effective head remains `f1b42b5` because the push changes only the metadata set.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** This verdict applies to head `f1b42b5`. The provider gate passes, the complete diff has review coverage, all implementation and CI checks pass, and no blocking finding remains. The review-gate check needs this record on the PR branch before the owner merges.
