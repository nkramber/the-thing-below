# PR-30 review

Date: 2026-09-18

## Identity

- PR: 30
- Target: `main`
- Base: `3a7340f`
- Merge base: `3a7340f`
- Head: `d1d2a47`
- Branch: `feat/pr-43-storage-and-saves`

## Provider gate

The PR handoff identifies Claude Code as the author provider. The active reviewer is Codex. The providers are different, so the cross-provider gate passes under T-4 and D-17.

## Intended behavior and scope

The PR adds the Storage project, two-line save text, versioned snapshot reading, safe file replacement, platform save folders, and the boot folder check. The review inspected the complete diff from `3a7340f` through `d1d2a47`, the affected Core, Storage, Game, Tests, solution, project, skill, decision, roadmap, and handoff paths, the PR description, and the existing PR comment.

Affected contracts: Core boundary, determinism and replay compatibility, save errors and recovery, JSON parsing, platform folder rules, safe writes, Godot storage alignment, dependency declarations, tests, and the PR documents gate. No presentation or player-text contract changes apply.

Inspected paths: every path in `git diff --name-status 3a7340f..e3ff30b`. No changed path remains uninspected.

## Findings

No finding.

## Out of scope

The later Game save-load integration belongs to PR-16, and the crash and log file consumers belong to PR-44, as the roadmap and PR description state. These are not defects in the Storage and save-format implementation reviewed here.

## PR comments

- Gitar reported a review-gate validation failure and separately approved the code with no finding. The live PR still reports `review-gate` as failed because this review record did not exist when that run executed. The code approval covers the effective implementation head `d1d2a47`.

## Description edits

None.

## Verification

- `git fetch origin`: passed. `origin/main` is `3a7340f`, and the PR tip is `e3ff30b`.
- The only commit after effective head `d1d2a47` changes review or handoff metadata.
- `make verify` at `e3ff30b`: passed. Build had 0 warnings and 0 errors. The test run passed 536 tests. Format, `det-lint`, STE check, replay identity, content hash, Godot build, and the bounded smoke session passed.
- Godot smoke output: passed. It printed the `mobile` renderer, `1280 by 720` frame, matching save folder, content hash, state hash `0x82def31590ae6c3b`, and the success line before exit.
- GitHub CI for `e3ff30b`: reported passed for build, test, format on Ubuntu, Windows, and macOS, coverage, `det-lint`, replay identity, STE check, changed paths, and smoke.
- Gitar for `e3ff30b`: reported approved with no code finding.
- `review-gate` for `e3ff30b`: failed because the record targeted the wrong effective head. The record now targets `d1d2a47`; a fresh gate run is required after this metadata commit.
- Push: pending. The review record and handoff entry are not yet committed or pushed.

## Open questions and accepted risks

None. The PR records owner answers D-654 through D-657 and resolves OQ-186 through OQ-189.

## Verdict

**Ready for owner merge.** This verdict applies to head `d1d2a47`. The provider gate passes, the complete diff and affected contracts are covered, the local and reported implementation checks pass, and no blocking finding remains. The review-gate check must rerun against this metadata publication.
