# PR-27 review

Date: 2026-09-18

## Identity

- PR: 27
- Target: `main`
- Base: `772468a`
- Merge base: `772468a`
- Head: `a963e4f`
- Branch: `feat/pr-4-core-math-and-identity`

## Provider gate

The author provider is Anthropic. Sessions 105 and 106 in `docs/session-handoff.md` name Claude Code as author, and their commits hold the implementation and correction. The reviewer provider is OpenAI. The providers differ, so the gate passes (T-4, D-17).

## Intended behavior and scope

PR-4 adds fixed-point math, deterministic random streams, xxHash64 and state hashing, contextual errors and assertions, the replay identity set and command, and its CI and local checks. The review inspected all 38 paths in the merge-base-to-effective-head diff and read each changed code, test, workflow, build, and document path. The affected contracts are Core isolation, determinism, replay identity, errors, test coverage, CI inputs and permissions, dependency policy, and the PR document and handoff rules. Presentation, content loading, and player strings do not apply.

Inspected paths: `.github/pull_request_template.md`, `.github/workflows/ci.yml`, `AGENTS.md`, `CLAUDE.md`, `Makefile`, `TheThingBelow.Core/BasisPoints.cs`, `TheThingBelow.Core/CoreAssert.cs`, `TheThingBelow.Core/Hashing/StateHasher.cs`, `TheThingBelow.Core/Hashing/XxHash64.cs`, `TheThingBelow.Core/Identity/IdentitySet.cs`, `TheThingBelow.Core/RunContext.cs`, `TheThingBelow.Core/SimulationException.cs`, `TheThingBelow.Core/SimulationVersion.cs`, `TheThingBelow.Core/Streams/Pcg32.cs`, `TheThingBelow.Core/Streams/RandomStream.cs`, `TheThingBelow.Core/Streams/RandomStreams.cs`, `TheThingBelow.Core/Streams/StreamId.cs`, `TheThingBelow.Tests/BasisPointsTests.cs`, `TheThingBelow.Tests/Pcg32Tests.cs`, `TheThingBelow.Tests/RandomStreamsTests.cs`, `TheThingBelow.Tests/ReplayIdentityTests.cs`, `TheThingBelow.Tests/RunContextTests.cs`, `TheThingBelow.Tests/StateHasherTests.cs`, `TheThingBelow.Tests/ToolsCommandLineTests.cs`, `TheThingBelow.Tests/XxHash64Tests.cs`, `TheThingBelow.Tests/identity/replay-identity.txt`, `TheThingBelow.Tools/Identity/IdentityFile.cs`, `TheThingBelow.Tools/Identity/ReplayIdentityCommand.cs`, `TheThingBelow.Tools/Program.cs`, `TheThingBelow.Tools/TheThingBelow.Tools.csproj`, `docs/decisions.md`, `docs/design.md`, `docs/questions.md`, `docs/roadmaps/area-ci.md`, `docs/roadmaps/area-core.md`, `docs/roadmaps/phase-1-foundations.md`, `docs/runbooks/dev-machine.md`, and `docs/session-handoff.md`.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar reported that malformed identity lines escaped the command as unhandled `InvalidDataException`. The author added that type to the command's file-fault filter in `a963e4f` and added command-path tests for three malformed lines. I verified the catch path and regression tests; the current Gitar dashboard approves the reviewed code, and its thread is resolved.

## Description edits

None.

## Verification

- `git diff --check 772468a..a963e4f`: passed.
- `make verify` on the PR checkout: passed. Build had 0 warnings and 0 errors; 290 tests passed; format, `det-lint`, `ste-check`, replay identity, and the Godot smoke session passed.
- `gh pr checks 27`: 13 CI checks pass on tip `0cc4ddc`, including build, test, format, smoke, replay identity on three legs, `det-lint`, `ste-check`, coverage, and changed paths. The code-bearing effective head remains `a963e4f` (D-610).
- `review-gate` on `0cc4ddc`: fails only RG 3 because `docs/reviews/pr-27.md` was not yet on the branch. The run log confirms no other fault.
- `review-gate` on `e1bfc72`: passed after publication of the review record.
- Gitar current-pass check: dashboard `5731578321` was updated at 14:37:25Z, after the 14:37:02Z `On it` reply. It approves the code and has no open finding. The review thread is resolved.
- `AGENTS.md` and `CLAUDE.md` are byte-identical.
- Push: `7969d87` was the head of `origin/feat/pr-4-core-math-and-identity`, verified with `gh pr view`.

## Open questions and accepted risks

No open questions affect this PR. OQ-60, OQ-61, and OQ-62 close with D-641 to D-645. The Release assertion check passes in the CI Release test job. The review does not verify a Release build on this Mac.

## Verdict

**Ready for owner merge.** This verdict applies to head `a963e4f`. The review verified the complete PR scope, the regression response, and all required code checks. CI and the current Gitar pass cover the same code, and `review-gate` passed after publication.
