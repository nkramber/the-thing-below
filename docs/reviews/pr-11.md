# PR-11 review

Date: 2026-09-16

## Identity

- PR: 11
- Target: `main`
- Base: `63803d9cd1855cc0d3aa4205be4ec13fd8430b54`
- Merge base: `63803d9cd1855cc0d3aa4205be4ec13fd8430b54`
- Head: `6266d54d1355d1c7454349ff71a92276a2b55d8f`
- Branch: `docs/pr-11-roadmaps`

## Provider gate

The Session 33 handoff identifies Claude Code as the provider that made the substantive PR changes. The active reviewer is Codex, which is the opposite provider. The gate passes under T-4 and D-17.

## Intended behavior and scope

The PR adds twelve area roadmaps, five phase roadmaps, the roadmaps index, and the rebuild of sections 7 and 8 of `docs/design.md`. It also updates the decision register, questions register, skills, agent guidance, PR template, and runbooks. The repeat review inspected the response, the correction diff through effective head `6266d54`, the design and decision contracts, the affected roadmap files, prior review records, the handoff archive, the PR description, and every PR comment. No code or player content changes in this PR.

## Findings

### P1-1: PR-61 requires tests that its prerequisites schedule later

Status: fixed in `6266d54`.

File: `docs/roadmaps/phase-2-first-playable.md:117-125`.

Trigger: The original head required a screen test before PR-41 created the screen-test job. The original panel wording also said each panel while PR-62 owned the window stack.

Expected: Each PR can pass its stated exit tests when it lands, and PR-61 is limited to the UI base while menu windows remain out of scope. The roadmap must keep its prerequisites and exit tests in a valid order (D-144, D-524, D-525, G-16).

Actual: The corrected roadmap replaces the PR-61 capture with a headless fit computation, names the fixture panel, and moves the captures to PR-41. PR-61 explicitly places the frame and fit captures out of scope, and PR-41 now owns their exit test. PR-62 still owns the window stack, while PR-61 owns the fixture-panel sizing rule.

Consequence: The original sequencing defect is removed. The fixture-panel test has a concrete fixture owner in PR-61, and the screen capture has its job owner in PR-41.

Correction: Verified. PR-61 covers the fit computation and fixture panel. PR-41 covers the screen captures. PR-62 remains the owner of the window stack.

Regression check: The author’s dependency scan and an independent read of the Phase 2 and UI-area sequences show no PR-61 exit test depends on PR-41 or PR-62.

### P2-1: D-543 and D-544 assign the condition format to different PRs

Status: fixed in `6266d54`.

File: `docs/decisions.md:683-684`.

Trigger: The original D-543 row assigned the shared condition form to PR-18. D-544 assigned it to PR-68 without a revision marker.

Expected: A later change to an owner decision marks the earlier decision as revised in part or superseded, and citations identify the revision (D-19, D-17).

Actual: The corrected Effect column of D-543 explicitly says that D-544 revises the owner of the condition form to PR-68 and that PR-18 keeps the branches and choice effects. The one-form rule and its readers remain unchanged.

Consequence: The decision register now has one condition-format owner and a traceable revision.

Correction: Verified. D-543 names its partial revision by D-544 and preserves the standing parts.

Regression check: The author searched all tracked documents. Roadmap citations either cite the one-form rule or name PR-68, and none claims that PR-18 defines the form.

## Out of scope

- The implementation quality of the future PRs belongs to their individual reviews. This PR sets their roadmap contracts only.
- The unresolved implementation choices in OQ-60 to OQ-178 belong to the PRs that the questions register names. None blocks this review.

## PR comments

- The owner requested `Gitar review`. The review verified the resulting automated pass comment.
- Gitar reported that it was running the requested pass. The review verified the resulting approval comment.
- Gitar reported one citation finding on the Session 33 handoff. The author changed D-67 to D-66, replied, and resolved the thread. The review verified the correction and found no open automated comment.
- The owner requested another `Gitar review` after the corrections. The review verified the approval comment on corrected head `6266d54`, with no open finding or new comment.

## Description edits

None.

## Verification

- `gh pr view 11 --json number,baseRefOid,headRefOid,baseRefName,headRefName,author,files,statusCheckRollup`: passed. The PR targets `main`, has base `63803d9`, and its remote tip is `7899972`, with no status checks reported.
- `git merge-base 63803d9 origin/docs/pr-11-roadmaps`: passed. The merge base equals the PR base.
- Effective-head check under D-184: `6266d54` is the newest commit that changes a path outside `docs/reviews/`, `docs/session-handoff.md`, and `docs/session-handoff-archive.md`. The later commit `7899972` changes only `docs/session-handoff.md`. The earlier review metadata commits are also excluded.
- `git diff --name-status 63803d9..6266d54`: passed. The substantive scope contains the original roadmap files and the corrected decision and phase files, with no code or player content.
- `git diff 848de1e..6266d54 -- docs/decisions.md docs/roadmaps/phase-2-first-playable.md`: passed. The two original findings and the newly found PR-68 dependency are corrected.
- `git diff --check 63803d9..7899972`: passed.
- `cmp -s AGENTS.md CLAUDE.md`: passed. The files are identical.
- `python3 docs/tools/ste-check.py $(git ls-files '*.md' | grep -v -e '^docs/reviews/' -e '^docs/session-handoff' -e '^docs/archive/')`: passed with 0 findings.
- `gh pr checks 11`: no checks reported. The repository has no executable build, test, format, screen-test, det-lint, replay-identity, smoke, bot, night-gate, or review-gate checks yet. The PR names the creating PR for each under G-16.
- Exit-test dependency scan: passed. No phase exit test depends on a later PR that creates its named tool or job. The two remaining textual matches are screen-test captures in PR-59 and PR-37, both after PR-41.
- PR-id coverage and order checks: passed. Every active id from PR-1 to PR-79 still appears once in a phase file and once in a phase sequence. The phase lists, global sequence, and area sequences remain aligned.
- Build and test commands: not run because this repository has no solution, code, Makefile, or executable checks yet. PR-1 and later PRs create them under G-16.
- Push: `7899972` is the head of origin/docs/pr-11-roadmaps, verified with `gh pr view`.

## Open questions and accepted risks

- OQ-60 to OQ-178 remain open and block the later PRs named in `docs/questions.md`. No open question blocks this documentation review.
- The absence of executable checks is an accepted initial-plan limitation under G-16. PR-1 and later PRs create the checks named in the PR gate.

## Earlier verdicts

- `Changes required` at `848de1e` for P1-1 and P2-1.

## Verdict

**Ready for owner merge.** This verdict applies to head `6266d54d1355d1c7454349ff71a92276a2b55d8f`. Both prior findings are fixed and the additional later-dependency scan is clean. The provider gate, complete review coverage, and applicable document checks pass.
