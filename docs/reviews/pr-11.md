# PR-11 review

Date: 2026-09-16

## Identity

- PR: 11
- Target: `main`
- Base: `63803d9cd1855cc0d3aa4205be4ec13fd8430b54`
- Merge base: `63803d9cd1855cc0d3aa4205be4ec13fd8430b54`
- Head: `848de1ea3ccbc77584cb29f44af58efa9d1df125`
- Branch: `docs/pr-11-roadmaps`

## Provider gate

The Session 33 handoff identifies Claude Code as the provider that made the substantive PR changes. The active reviewer is Codex, which is the opposite provider. The gate passes under T-4 and D-17.

## Intended behavior and scope

The PR adds twelve area roadmaps, five phase roadmaps, the roadmaps index, and the rebuild of sections 7 and 8 of `docs/design.md`. It also updates the decision register, questions register, skills, agent guidance, PR template, and runbooks. The review inspected the complete diff through effective head `848de1e`, the design and decision contracts, the questions register, all new roadmap files, the applicable skills, prior review records, the handoff archive, the PR description, and every PR comment. No code or player content changes in this PR.

## Findings

### P1-1: PR-61 requires tests that its prerequisites schedule later

Status: open.

File: `docs/roadmaps/phase-2-first-playable.md:117-125`.

Trigger: Follow the stated Phase 2 order. PR-61 lands before PR-41, which creates the screen-test job, and before PR-62, which creates the menu windows.

Expected: Each PR can pass its stated exit tests when it lands, and PR-61 is limited to the UI base while menu windows remain out of scope. The roadmap must keep its prerequisites and exit tests in a valid order (D-144, D-524, D-525, G-16).

Actual: PR-61 requires a screen test capture and a test that each panel holds the longest string, but its screen-test job is PR-41 and its panels are out of scope until PR-62. The area roadmap repeats the same split: PR-41 owns the screen-test job, while PR-62 owns the window stack and menu screens (`docs/roadmaps/area-ui-input.md:215-236`).

Consequence: The roadmap cannot satisfy PR-61's required exit tests at the point where its sequence requires the PR to merge. The owner must either move those tests to PR-41 and PR-62, respectively, or expand PR-61 and move the dependencies with it.

Correction: Make the PR-61 exit tests cover only the UI base, or assign the screen capture to PR-41 and the panel-sizing test to PR-62. Align the area roadmap and its sequence with the correction.

Regression check: Re-read the Phase 2 and UI-area sequences and verify that every PR-61 exit test has its implementation and test owner before PR-61. The check must show no PR-61 exit test depends on PR-41 or PR-62.

### P2-1: D-543 and D-544 assign the condition format to different PRs

Status: open.

File: `docs/decisions.md:683-684`.

Trigger: Resolve the decision register in order. D-543 says PR-18 defines the shared condition form. The later D-544 says PR-68 takes the condition form and says that it applies to D-543.

Expected: A later change to an owner decision marks the earlier decision as revised in part or superseded, and citations identify the revision (D-19, D-17).

Actual: D-544 does not mark D-543 as revised in part. The register therefore assigns the same condition format to PR-18 and PR-68, while the phase and area roadmaps follow only the D-544 assignment.

Consequence: The decision register has two incompatible implementation owners. A later PR can use D-543 to claim that PR-18 must define the parser, which can split the format or duplicate its validation.

Correction: Add an explicit revision note to D-543, or correct the D-543 row so that it names PR-68, and state that PR-18 keeps only the branch and choice behavior.

Regression check: Search the decision register and all roadmap citations for the condition format owner. The result must identify PR-68 as the sole owner and name D-544 wherever D-543's ownership text is superseded.

## Out of scope

- The implementation quality of the future PRs belongs to their individual reviews. This PR sets their roadmap contracts only.
- The unresolved implementation choices in OQ-60 to OQ-178 belong to the PRs that the questions register names. None blocks this review.

## PR comments

- The owner requested `Gitar review`. The review verified the resulting automated pass comment.
- Gitar reported that it was running the requested pass. The review verified the resulting approval comment.
- Gitar reported one citation finding on the Session 33 handoff. The author changed D-67 to D-66, replied, and resolved the thread. The review verified the correction and found no open automated comment.

## Description edits

None.

## Verification

- `gh pr view 11 --json number,baseRefOid,headRefOid,baseRefName,headRefName,author,files,statusCheckRollup`: passed. The PR targets `main`, has base `63803d9`, and its remote tip is `e92e329`, with no status checks reported.
- `git merge-base 63803d9 origin/docs/pr-11-roadmaps`: passed. The merge base equals the PR base.
- Effective-head check under D-184: `848de1e` is the newest commit that changes a path outside `docs/reviews/`, `docs/session-handoff.md`, and `docs/session-handoff-archive.md`. The later commits `9e41a06`, `1bca609`, and `e92e329` change only `docs/session-handoff.md`.
- `git diff --name-status 63803d9..848de1e`: passed. The complete substantive scope contains the 31 files listed by the PR, with no code or content files.
- `git diff --check 63803d9..848de1e`: passed.
- `cmp -s AGENTS.md CLAUDE.md`: passed. The files are identical.
- `python3 docs/tools/ste-check.py $(git ls-files '*.md' | grep -v -e '^docs/reviews/' -e '^docs/session-handoff' -e '^docs/archive/')`: passed with 0 findings.
- `gh pr checks 11`: no checks reported. The repository has no executable build, test, format, screen-test, det-lint, replay-identity, smoke, bot, night-gate, or review-gate checks yet. The PR names the creating PR for each under G-16.
- Build and test commands: not run because this repository has no solution, code, Makefile, or executable checks yet. PR-1 and later PRs create them under G-16.
- Push: pending publication of this review record and handoff. The final remote head will be verified with `gh pr view`.

## Open questions and accepted risks

- OQ-60 to OQ-178 remain open and block the later PRs named in `docs/questions.md`. No open question blocks this documentation review.
- The absence of executable checks is an accepted initial-plan limitation under G-16. PR-1 and later PRs create the checks named in the PR gate.

## Verdict

**Changes required.** This verdict applies to head `848de1ea3ccbc77584cb29f44af58efa9d1df125`. The provider gate passes, but PR-61 has exit tests scheduled before their owners exist, and D-543 conflicts with D-544 without an explicit revision.
