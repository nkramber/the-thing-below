# PR-22 review

Date: 2026-09-17

## Identity

- PR: 22
- Target: `main`
- Base: `9787b2d65ad31d8faf44018c6725af330518a3aa`
- Merge base: `9787b2d65ad31d8faf44018c6725af330518a3aa`
- Head: `1f07b6b9a650f59080e1b4bced9c371b5001ffec`
- Branch: `feat/pr-84-context-budget`

## Provider gate

The author provider is Claude Code, as Sessions 86 and 87 in `docs/session-handoff.md` state. The active reviewer provider is Codex. The providers differ, so this review meets T-4 and D-17.

## Intended behavior and scope

PR-84 adds three size checks to `ste-check`: the two instructions files, the top handoff entry, and each skill file. It also adds tests and records the limits and scope. The review inspected all 14 paths in `git diff --stat 9787b2d...1f07b6b`: `.claude/skills/ste-writing/SKILL.md`, `.github/pull_request_template.md`, `AGENTS.md`, `CLAUDE.md`, `TheThingBelow.Tests/SteCheckCheckout.cs`, `TheThingBelow.Tests/SteCheckSizeTests.cs`, `TheThingBelow.Tools/SteCheck/SessionNumberRules.cs`, `TheThingBelow.Tools/SteCheck/SizeRules.cs`, `TheThingBelow.Tools/SteCheck/SteCheckCommand.cs`, `docs/decisions.md`, `docs/roadmaps/area-ci.md`, `docs/roadmaps/area-tools.md`, `docs/session-handoff-archive.md`, and `docs/session-handoff.md`. No changed area remains uninspected.

The review checked D-10, D-18, D-20, D-21, D-22, D-583 to D-591, D-603, D-605, D-607, D-608, D-610, D-611, D-613, T-2 to T-6, and L-12. It traced the byte count, file selection, handoff boundary, findings, and command exit path. The PR matches the five exit tests in section 7.6 of `docs/roadmaps/phase-1-foundations.md`. It also meets the Documents gate in the PR body.

## Findings

No finding.

## Out of scope

None.

## PR comments

- The Gitar comment on the first head claimed that SIZE 2 used a looser heading match than HANDOFF rules. The author fixed this in `1f07b6b` by sharing `SessionNumberRules.IsSessionHeading`. `ASubHeadingInsideTheTopEntryDoesNotEndIt` verifies the former trigger; the test passes on the reviewed head and would fail under the old prefix matcher.

## Description edits

- Changed the test count from 135 to 136 in the summary and PR gate. Local verification passed 136 tests, of which 13 belong to `SteCheckSizeTests.cs`.

## Verification

- `git diff --check 9787b2d...1f07b6b`: passed.
- `dotnet build TheThingBelow.slnx`: passed, 0 warnings and 0 errors.
- `dotnet test --solution TheThingBelow.slnx --no-build -- --filter-not-trait "Category=Smoke"`: passed, 136 tests.
- `dotnet format TheThingBelow.slnx --verify-no-changes`: passed.
- `dotnet run --project TheThingBelow.Tools/TheThingBelow.Tools.csproj -- ste-check --root .`: passed, 0 findings.
- CI on `1f07b6b`: three build/test/format legs, three smoke legs, changed paths, coverage report, and `ste-check` passed. `review-gate` failed RG 3 because this record was not yet on the PR head; RG 1, RG 2, RG 6, RG 7, and RG 8 passed, and RG 4–5 skipped after RG 3. The other CI jobs that do not exist yet are named with their creating PR in the PR gate, as G-16 requires.
- `make verify` and Godot smoke session were not rerun locally; Session 87 reports both passed on `1f07b6b`, and CI smoke passed on all three legs.
- Push: pending.

## Open questions and accepted risks

No open question affects this PR. The `det-lint`, `replay-identity`, `screen-test`, bot, and `night-gate` jobs do not exist yet; the PR gate names the PR that creates each one (G-16). These checks remain future gates under the roadmap.

## Verdict

**Ready for owner merge.** This verdict applies to head `1f07b6b9a650f59080e1b4bced9c371b5001ffec`. The implementation, tests, applicable local checks, and CI evidence pass. The current `review-gate` failure is RG 3 alone because the review record has not yet reached the PR head.
