# PR-46 review

Date: 2026-09-21

## Identity

- PR: 46
- Target: `main`
- Base: `c6cc71c`
- Merge base: `c6cc71c`
- Head: `c480baa`
- Branch: `feat/pr-9-battle-core`

## Provider gate

The author provider is Claude Code. The Session 158 handoff names Claude Code as the author. The reviewer provider is Codex. The providers differ, so the gate passes T-4 and D-17.

## Intended behavior and scope

The PR adds the deterministic battle core, timeline, actions, rows, waves, wipe reload, save and record formats, fixture content, debug commands, and Game event playback. The review covered Core boundary, determinism, replay, errors, content, gameplay, presentation integration, persistence, and the PR-9 roadmap exit tests.

Inspected paths from `git diff --stat c6cc71c...c480baa`: `.claude/skills/ste-writing/references/glossary.md`; all changed files under `TheThingBelow.Core/Battles/`, `TheThingBelow.Core/Content/`, `TheThingBelow.Core/Identity/`, `TheThingBelow.Core/Logging/`, `TheThingBelow.Core/Maps/`, `TheThingBelow.Core/Runs/`, `TheThingBelow.Core/Saves/`, and `TheThingBelow.Core/SimulationVersion.cs`; all changed files under `TheThingBelow.Debug/Commands/`; `TheThingBelow.Game/scripts/BattleEventQueue.cs`, `BattleEventQueue.cs.uid`, `Boot.cs`, and `GameRun.cs`; all changed files under `TheThingBelow.Tests/`; `content/rules/battle.json`; `content/rules/fixtures/battle.json`; and all changed files under `docs/decisions.md`, `docs/design.md`, `docs/questions.md`, `docs/roadmaps/`, `docs/runbooks/`, `docs/session-handoff.md`, and `docs/session-handoff-archive.md`. No changed path remains uninspected.

## Findings

No finding.

## Out of scope

The battle screen, enemy records, elements, statuses, evaluator, and balance work belong to PR-10, PR-80, PR-66, PR-11, and PR-30 as stated by the roadmap. None blocks this review.

## PR comments

None. The exported issue, review, and inline-comment sets contain no comment thread. Gitar passed the reviewed head.

## Description edits

None.

## Verification

- `git fetch origin`: passed. The local checkout matches the PR branch.
- `git diff --stat c6cc71c...c480baa`: passed. The complete 78-path diff was inspected.
- `make verify` at `c480baa`: passed. Build, 1,485 tests, format, det-lint, STE, replay identity, content hash, atlas, and smoke passed.
- Focused battle tests at `c480baa`: passed. The 1,000-seed timeline, row, wipe, event queue, map freeze, flee, wave, save, and replay checks passed.
- GitHub CI at `c480baa`: passed. Build, test, format, smoke, replay identity, screen-test, det-lint, STE, coverage, changed paths, and Gitar passed.
- GitHub `review-gate` at `c480baa`: failed only because this review record did not yet exist.
- `gh pr view 46 --json headRefOid --jq .headRefOid`: passed. The remote head is `c480baaba30eafbf4ef3f3df653ca43e82256045`.
- Push: `d837238` is the head of `origin/feat/pr-9-battle-core`, verified with `gh pr view`.

## Open questions and accepted risks

None. The PR-9 questions are resolved by D-755 to D-781.

## Verdict

**Ready for owner merge.** This verdict applies to head `c480baa`. The opposite-provider review covers the complete diff, the required exit tests pass, and no blocking finding remains. The owner merge remains the final action.
