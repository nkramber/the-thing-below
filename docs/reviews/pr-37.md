# PR-37 review

Date: 2026-09-19

## Identity

- PR: 37
- Target: `main`
- Base: `51a040fd8913216f4339f94ca741296382f50d6d`
- Merge base: `51a040fd8913216f4339f94ca741296382f50d6d`
- Head: `9927be7`
- Branch: `fix/pr-88-ci-matrix-check-names`

## Provider gate

The PR handoff and PR description identify Claude Code as the author provider. The active reviewer is Codex. The providers differ, so the cross-provider gate passes under T-4 and D-17.

## Intended behavior and scope

The PR adds one stable required check for each CI matrix family. Each gate reads the result of `changed-paths` and its matrix job. The review inspected the workflow, the workflow tests, the affected decisions, questions, roadmaps, design register, and handoff records.

Inspected paths:

- `.github/workflows/ci.yml`
- `TheThingBelow.Tests/CiWorkflowGateTests.cs`
- `docs/decisions.md`
- `docs/design.md`
- `docs/questions.md`
- `docs/roadmaps/area-ci.md`
- `docs/roadmaps/phase-1-foundations.md`
- `docs/session-handoff.md`
- `docs/session-handoff-archive.md`

No affected area remains uninspected.

## Findings

No finding.

## Out of scope

None.

## PR comments

- The author answered the automated pass claim that the Documents section had an invalid review-record line. The review verified the corrected `docs/reviews/` line and the stated RG 7 result. The automated pass approved the code review at head `9927be7`, which is the effective implementation head.
- The automated pass reported no code thread. The review independently verified the workflow behavior and did not rely on that approval.

## Description edits

None.

## Verification

- `git fetch origin`: passed. Base is `51a040f` and the effective implementation head is `9927be7`.
- `git diff --stat 51a040f..8a041e3`: passed. The complete nine-path diff was inspected.
- `dotnet test TheThingBelow.Tests/TheThingBelow.Tests.csproj --no-restore --filter FullyQualifiedName~CiWorkflowGateTests`: passed, 12 tests.
- `make verify` at the PR tip: passed, build, 806 tests, format, det-lint, STE, replay identity, content hash, and bounded smoke.
- `gh pr checks 37`: all CI checks passed except `review-gate`, which correctly fails because this review record is not yet on the PR head. The Windows matrix checks completed successfully after the initial snapshot.
- Gitar: current approved pass recorded by the author for effective head `9927be7`, with no open code thread.
- Push: `8a041e3bfd1e2eb685dcf31ceb5061f782c40cc0` is the head of `origin/fix/pr-88-ci-matrix-check-names`, verified with `gh pr view`.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** This verdict applies to head `9927be7`.
The workflow preserves the docs-only skip condition, fails gates on unexpected skips and upstream faults, and reports stable required names. The focused tests and all available local and revision-matched CI checks pass.
