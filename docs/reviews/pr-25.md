# PR-25 review

Date: 2026-09-18

## Earlier verdicts

**Changes required.** This verdict applied to head `83c17f7a345c36183ae7d17301d0ff9e866c1ba7`. P2-1 found two Phase 1 steps with the same sequence number.

## Identity

- PR: 25
- Target: `main`
- Base: `b3ec2b448daf7e13a889612e3029297f7ea1246a`
- Merge base: `b3ec2b448daf7e13a889612e3029297f7ea1246a`
- Head: `ac34b5fd1ff25174dfb5226fddbac2c001a4d5f4`
- Branch: `docs/pr-86-screen-scale-answers`

## Provider gate

Session 99 names Claude Code as the author provider for this branch. Codex is the reviewer provider. The providers differ, so the gate passes (T-4, D-17).

## Intended behavior and scope

PR-86 records the screen scale answers, updates the design and UI contracts, revises the roadmaps, and closes OQ-183. I inspected the complete diff from the merge base to the effective head, the PR description, the applicable decisions, and section 7.9 of the phase roadmap. The review covers document consistency, the one-PR sequence, the text limits, and the screen scale contract (D-228, D-568, D-633 to D-640, D-577, D-581).

Inspected paths: `.claude/skills/csharp-conventions/SKILL.md`, `.claude/skills/game-text-style/SKILL.md`, `.claude/skills/ste-writing/SKILL.md`, `.claude/skills/ste-writing/references/glossary.md`, `docs/decisions.md`, `docs/design.md`, `docs/questions.md`, `docs/reviews/pr-25-response.md`, `docs/reviews/pr-25.md`, `docs/roadmaps/area-art.md`, `docs/roadmaps/area-effects.md`, `docs/roadmaps/area-ui-input.md`, `docs/roadmaps/phase-1-foundations.md`, `docs/roadmaps/phase-2-first-playable.md`, `docs/roadmaps/phase-5-first-release.md`, `docs/session-handoff-archive.md`, and `docs/session-handoff.md`.

No changed path remains uninspected. The spike folders remain untracked and outside this PR.

## Findings

### P2-1: The Phase 1 sequence repeats item 11

Status: fixed in `ac34b5f`.

File: `docs/roadmaps/phase-1-foundations.md:806`.

Trigger: Read the Phase 1 sequence after PR-86 was inserted.

Expected: Each item has a unique number that preserves the strict order of the phase (D-144, D-488).

Actual: The prior head gave PR-46 and the owner step to require the checks on `main` the number 11. The corrected sequence gives each item a unique number from 1 to 23, in order.

Consequence: A reader cannot use the item number to identify one step in the Phase 1 order.

Correction: Renumber the owner step and each later item in this sequence. Keep the order and the PR-86 entry.

Regression check: The saved ordered-list sweep reports no duplicate number in any list. Both Phase 1 lists run from 1 to 23. The gate reference now names section 7.20, and the decision range ends at D-640. `ste-check` reports 0 findings.

## Out of scope

None.

## PR comments

The current Gitar dashboard approves the effective head, with two closed findings and none open. The review-thread query and the issue-comment and pull-request-review exports contain both claims, the author's replies, and Gitar's confirmation. The duplicate dialogue box bullet is gone, and the PR-86 entry lists F-68 to F-77 and four screens.

## Description edits

None.

## Verification

- `git fetch origin`: passed. `origin/main` is `b3ec2b4`; local and remote branch tips match.
- `git diff --stat b3ec2b4..ac34b5f`: passed. All 17 paths were inspected, including 13 implementation paths and the four metadata paths (D-610).
- `git diff 83c17f7..ac34b5f`: passed. The correction changes the Phase 1 sequence in `docs/design.md` and `docs/roadmaps/phase-1-foundations.md`.
- Ordered-list sweep and reference searches: passed. No repeated item number, stale section 7.19, or stale D-626 to D-638 range remains.
- `git diff --check b3ec2b4..ac34b5f`: passed.
- `make verify`: passed at local head `118551e`, with 0 build warnings, 187 tests, format, det-lint, ste-check, and smoke.
- Gitar: current approval of effective head `ac34b5f`; dashboard edit `5726614038` follows the `On it` reply, and both threads are resolved.
- PR comments: passed. The review-thread query, issue-comment export, and pull-request-review export contain both Gitar claims, both author replies, and Gitar's confirmation.
- `gh pr checks 25`: `changed paths` and `ste-check` pass. Docs-only build, test, format, coverage, det-lint, and smoke checks skip under D-600. `review-gate` fails RG 4 and RG 5 because this record still holds the old verdict and head.
- Push: pending publication of this review record and handoff entry.

## Open questions and accepted risks

The local `make verify` run used the metadata tip `118551e`, while the reviewed effective head is `ac34b5f`; the implementation diff is identical under D-610. The docs-only CI skips build, test, format, coverage, det-lint, and smoke. The worktree also contains untracked `deck-test/` and `screen-scale-probe/` directories, which are outside the PR.

## Verdict

**Ready for owner merge.** This verdict applies to head `ac34b5fd1ff25174dfb5226fddbac2c001a4d5f4`. P2-1 is fixed, the full local verification passes, and the current Gitar pass approves the same effective head.
