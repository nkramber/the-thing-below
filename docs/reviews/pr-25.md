# PR-25 review

Date: 2026-09-18

## Identity

- PR: 25
- Target: `main`
- Base: `b3ec2b448daf7e13a889612e3029297f7ea1246a`
- Merge base: `b3ec2b448daf7e13a889612e3029297f7ea1246a`
- Head: `83c17f7a345c36183ae7d17301d0ff9e866c1ba7`
- Branch: `docs/pr-86-screen-scale-answers`

## Provider gate

Session 99 names Claude Code as the author provider for this branch. Codex is the reviewer provider. The providers differ, so the gate passes (T-4, D-17).

## Intended behavior and scope

PR-86 records the screen scale answers, updates the design and UI contracts, revises the roadmaps, and closes OQ-183. I inspected the complete diff from the merge base to the effective head, the PR description, the applicable decisions, and section 7.9 of the phase roadmap. The review covers document consistency, the one-PR sequence, the text limits, and the screen scale contract (D-228, D-568, D-633 to D-640, D-577, D-581).

Inspected paths: `.claude/skills/csharp-conventions/SKILL.md`, `.claude/skills/game-text-style/SKILL.md`, `.claude/skills/ste-writing/SKILL.md`, `.claude/skills/ste-writing/references/glossary.md`, `docs/decisions.md`, `docs/design.md`, `docs/questions.md`, `docs/roadmaps/area-art.md`, `docs/roadmaps/area-effects.md`, `docs/roadmaps/area-ui-input.md`, `docs/roadmaps/phase-1-foundations.md`, `docs/roadmaps/phase-2-first-playable.md`, `docs/roadmaps/phase-5-first-release.md`, `docs/session-handoff-archive.md`, and `docs/session-handoff.md`.

No changed path remains uninspected. The spike folders remain untracked and outside this PR.

## Findings

### P2-1: The Phase 1 sequence repeats item 11

Status: open.

File: `docs/roadmaps/phase-1-foundations.md:806`.

Trigger: Read the Phase 1 sequence after PR-86 was inserted.

Expected: Each item has a unique number that preserves the strict order of the phase (D-144, D-488).

Actual: PR-46 and the owner step to require the checks on `main` both have number 11. The owner step follows PR-86 in the list, so the duplicate leaves the sequence index ambiguous.

Consequence: A reader cannot use the item number to identify one step in the Phase 1 order.

Correction: Renumber the owner step and each later item in this sequence. Keep the order and the PR-86 entry.

Regression check: Read the full Phase 1 sequence and confirm that each number occurs once and follows the listed order. Run `ste-check` to confirm the document still passes its writing and reference rules.

## Out of scope

None.

## PR comments

The Gitar dashboard comment reports two findings, both closed on `83c17f7`: a duplicate dialogue box bullet and a stale PR-86 roadmap entry. The diff confirms that the duplicate is gone and that the entry lists F-68 to F-77 and four screens. The comment export did not return the inline replies, so their text remains unverified.

## Description edits

None.

## Verification

- `git diff --stat b3ec2b448daf7e13a889612e3029297f7ea1246a...83c17f7a345c36183ae7d17301d0ff9e866c1ba7`: passed. The stat lists 15 changed paths.
- `gh pr view 25 --json ...`: passed. GitHub reports base `b3ec2b4`, tip `9c13790`, and the expected branch. The effective head is `83c17f7`; the later commit changes only the handoff metadata (D-610).
- `gh pr checks 25`: `changed paths` and `ste-check` pass. Build, test, format, coverage, det-lint, and smoke checks skip for this docs-only PR (D-600). `review-gate` fails because the review record does not yet exist.
- `gh pr view 25 --comments`: passed for the Gitar dashboard comment. The REST export of issue comments, reviews, and inline comments failed because the GitHub API connection failed.
- `make verify`: failed at build after 5:00 with 0 warnings, 0 errors, and no diagnostics. The later steps did not run.
- `git fetch origin`: unavailable because `.git/FETCH_HEAD` is read-only in this environment.
- `make where`: unavailable because its `gh pr status` command could not connect to GitHub.
- `dotnet run --project TheThingBelow.Tools/TheThingBelow.Tools.csproj -- ste-check --root .`: passed with 0 findings after the review record and handoff edits.
- `dotnet` tests, format, and smoke session: not completed separately because the build failed.
- Push: `9c13790` is the head of `origin/docs/pr-86-screen-scale-answers`, verified with `gh pr view`.

## Open questions and accepted risks

The local build and full `make verify` result remain unverified. The author reports a successful run in Session 99. The inline replies to the two closed Gitar findings remain unverified because the API export failed. The effective head and current remote tip came from live `gh pr view` data; the read-only Git metadata prevented a fetch and publication of this review.

## Verdict

**Changes required.** This verdict applies to head `83c17f7a345c36183ae7d17301d0ff9e866c1ba7`. P2-1 leaves two Phase 1 steps with the same sequence number, so the roadmap order needs correction before merge.
