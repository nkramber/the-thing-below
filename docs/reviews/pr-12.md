# PR-12 review

Date: 2026-09-16

## Identity

- PR: 12
- Target: `main`
- Base: `6910017965a1993d41a1d4a68eb86d490ee3847b`
- Merge base: `c4d39fe70dfd6199740ba4948e183d66fe0ce6b9`
- Head: `ff04f87824ae1cfae947fd05ec20f5afcf64a67b`
- Branch: `docs/pr-12-critic`

## Provider gate

Sessions 37, 39, and 41 of `docs/session-handoff.md` identify Claude Code as the author of the substantive changes and the review corrections. Codex performed the three reviews. The providers differ, so the gate of T-4 and D-17 passes.

## Intended behavior and scope

The PR records the critic pass of D-484 and D-490. It adds D-555 to D-575, updates the design and question registers, rebuilds the roadmap contracts, and updates the guidance files for those decisions. The review inspected the complete diff from merge base `c4d39fe` through effective head `0fbfa82`, every changed path, the decision effects, the open-question consumers, the five phase roadmaps, the twelve area roadmaps, the guidance changes, the PR description, and the PR comments.

The repeat review read `docs/reviews/pr-12-response.md`, reproduced every original trigger and regression check, inspected the correction diff from `84b4128` through `d31ae6b`, and checked the new PR-27 order against the world and roadmap contracts. Commits `aa3c5e2` and `c8e7e48` change only the review and handoff metadata paths. No code, solution, or executable test project exists, so no build or game test ran.

The second repeat review reproduced P2-6 and its regression check. It inspected the correction diff from `d31ae6b` through `0fbfa82`, then checked the full dungeon contract in the affected area files and exit tests. Commit `74f9477` changes only `docs/session-handoff.md`.

The third repeat review reproduced P2-7 and its regression check. It inspected the correction diff from `0fbfa82` through `ff04f87`, checked the adjacent group scopes in Phase 2, and verified the art, effect, and budget contracts against D-523 and D-575. Commits `54edbe5` and `ef5a8b0` change only the review and handoff metadata paths.

## Findings

### P1-1: The 1080p pixel rule cannot be satisfied

Status: fixed in `d31ae6b`.

File: `docs/decisions.md:708`, `docs/questions.md:329-332`, `docs/roadmaps/area-ui-input.md:71-75`, and `docs/roadmaps/phase-2-first-playable.md:248-251`.

Trigger: Fit the 1280 by 720 frame to a 1920 by 1080 screen, as PR-61 and the PR-41 review focus require.

Expected: D-568 requires the art at 1920 by 1080 to have no blur and no uneven pixels. OQ-105 must offer a method that can meet that rule, and the roadmap exit and review checks must be possible to pass (T-2, D-144, and G-16).

Actual: The scale is 1.5. Nearest filtering must map two source pixels to three output pixels, so the output widths alternate. Linear filtering combines adjacent source pixels, so it adds blur. The recommended two-step method first makes a 2560 by 1440 nearest image and then reduces it to 1920 by 1080 with a linear ratio of 0.75. OQ-105 itself says that this method adds softness. Each listed option violates either the no-blur rule or the no-uneven-pixel rule.

Consequence: PR-61 cannot implement D-568 as written, and PR-41 cannot prove the required result. The Gate 2 screen evidence can only pass through an unstated relaxation of an owner decision.

Correction: Ask the owner to choose the 1080p tradeoff. Accept filtered softness, accept uneven nearest-neighbor pixels, or keep an exact integer scale with bars. Then update D-568, OQ-105, the PR-61 exit tests, the PR-41 review focus, and the screen rule in the guidance files together.

Regression check: For displays of 1280 by 800, 1920 by 1080, and 2560 by 1440, state the exact source-to-output pixel rule and the accepted visual property. The selected rule must not require both equal pixel widths and no sample mixing at a non-integer scale.

Repeat review: D-573 records the owner choice of even pixel sizes with slight edge softness. It sets the two-step method, revises D-568, resolves OQ-105, and updates the PR-61 and PR-41 contracts. The three-display table in the response matches the current roadmap.

### P2-1: Superseded frame contracts remain active

Status: fixed in `d31ae6b`.

File: `docs/decisions.md:303`, `docs/decisions.md:656`, `docs/decisions.md:664`, and `docs/questions.md:293-295`.

Trigger: Read the current contracts for the CRT pass, large pictures, the UI base, and the Deck effect-budget window after D-568 supersedes D-480.

Expected: A current consumer of a superseded decision names the superseding decision, and the current dimensions agree with D-568. The PR description also claims that no live document cites D-480 without D-568.

Actual: D-240 still runs the CRT pass on a 1280 by 800 frame. D-516 still sizes a full-screen picture at about 1422 by 800. D-524 still assigns PR-61 a 1280 by 800 frame and a separate 16:9 view, and its later D-561 note says that the rest stands. OQ-94 still cites D-480 and says that a 16:9 screen draws more than the Deck. None of these current contracts names D-568.

Consequence: PR-55, PR-56, PR-61, and PR-37 can implement the superseded two-view frame and create art or effects for the wrong dimensions. The checked claim in the PR description is also false.

Correction: Revise these live contracts for the one 1280 by 720 frame and cite D-568. Add an explicit revision note to each affected decision row. Correct the checked claim in the PR description after the scan passes.

Regression check: Search live documents for `1280 by 800`, `1422 by 800`, and `D-480`. Exclude dated records and text that describes the old decision. Every current consumer must use D-568 or name it as the superseding decision.

Repeat review: D-240, D-516, D-524, OQ-94, and the five adjacent contracts found by the author now name D-568 or state the current 1280 by 720 frame. The remaining scan hits are historical text with a revision mark, the Deck display size, or the Valve text-size rule.

### P2-2: Three new decisions leave their old rules in current consumers

Status: fixed in `d31ae6b`.

File: `docs/questions.md:357-359`, `docs/questions.md:420-422`, and `docs/roadmaps/phase-4-region-one.md:321-325,366-372`.

Trigger: Use OQ-114 for PR-7, OQ-135 for PR-67, or the PR-30 exit tests after D-555, D-566, and D-571.

Expected: Current questions and roadmap tests reflect the new owner decisions. D-566 removes fog of war. D-555 makes the save-point restore run once for the place until a story event reopens it. D-571 makes the owner set the M-4 band after M-4 and before the Gate 2 sign-off.

Actual: OQ-114 still says that fog covers every unseen tile. OQ-135 still says that a save point restores MP once for each visit. PR-30 still tests against a band that the Gate 2 sign-off set, although the owner sets it before the sign-off. Gate 4 also calls it the band of the Gate 2 sign-off.

Consequence: The implementation PRs can build removed fog state, reset the save point too often, or attribute the tuning band to the wrong step. Each result contradicts the decision that this PR adds.

Correction: Rewrite the OQ-114 prompt as a sight-only question, rewrite the OQ-135 prompt with the once-per-place reset rule, and make the PR-30 and Gate 4 tests name the owner-set band that exists before Gate 2. Scan the live questions and roadmap tests for the former rule of each decision.

Regression check: Searches for `fog covers`, `once for each visit`, and `band that the Gate 2 sign-off set` return no current contract. The remaining historical hits must carry explicit revision context.

Repeat review: OQ-114, OQ-135, PR-30, Gate 4, and D-42 now state D-555, D-566, and D-571 correctly. The searches return no current consumer of the former rules.

### P2-3: The scene-term rewrite assigns test instrumentation to a story scene

Status: fixed in `d31ae6b`.

File: `docs/questions.md:290-292`.

Trigger: Follow OQ-93 to build the throwaway Deck test scene of OQ-92.

Expected: D-572 requires each use of `scene` to name the correct kind. OQ-93 must assign the frame-time measurement code to the Deck test scene that the question describes.

Actual: The recommended option first says that the test scene measures itself, then says that the story scene needs its own measurement code. No story scene is in this test, and story scenes do not arrive until PR-68.

Consequence: The question assigns the measurement concern to the wrong component and leaves the test-scene requirement internally contradictory.

Correction: Change `The story scene needs its own measurement code` to `The test scene needs its own measurement code`.

Regression check: Read every changed replacement of bare `scene` in a non-story context. Each qualifier must match the component that the surrounding contract builds.

Repeat review: OQ-93 now assigns the measurement code to the test scene. The adjacent term scan found no other changed non-story use with the wrong qualifier.

### P2-4: The PR description attributes the critic work to an agent

Status: fixed in `d31ae6b`.

File: PR description, Summary and PR gate.

Trigger: Read the first two paragraphs of the PR description and its T-6 gate line.

Expected: A PR description names no provider, agent, harness, or model as the source of work (T-6, D-22, and the `pr-review` skill).

Actual: The description says that the `design-critic` agent read the plan in five slices and produced the five reports. The gate then claims that the PR has no attribution.

Consequence: The PR description violates an absolute tenet and its own checked gate line.

Correction: Describe the five-part critic pass and its reports without naming an agent as their source.

Regression check: Read the full PR description. It names no provider, agent, harness, or model as the source of the work.

Repeat review: The finding had partial merit. The PR description and the new dated design line no longer name an agent as the source of work. The term `design-critic pass` remains because D-484 names that plan item. The full PR description passes the attribution check.

### P2-5: The archived handoff sessions are not newest first

Status: fixed in `d31ae6b`.

File: `docs/session-handoff-archive.md:5-196` and Commit: `f39b08914ba81ed261e8b1f3ae567b51acd5992a`.

Trigger: Read the archive headings after Session 37 says that it moved Session 27 to the top of the archive.

Expected: D-18 requires archived sessions to stay newest first, and the Session 37 claim must match the committed move.

Actual: Commit `f39b089` inserted Session 27 below Sessions 26 and 24. After this review moved Session 28 to the top, the archive begins 28, 26, 24, 27, and 25.

Consequence: A reader who follows the archive instruction meets older state before newer state, and the Session 37 handoff gives a false account of its own archive update.

Correction: Move the existing archive blocks into strict descending session order without changing their text. Keep Session 28 at the top, then Sessions 27, 26, 25, and 24.

Regression check: `rg '^## Session ' docs/session-handoff-archive.md` lists every archived session in descending numeric order, with no duplicate and no missing archived session.

Repeat review: The archive runs from Session 29 through Session 1 in strict descending order. A block hash comparison proves that Sessions 1 through 38 kept their text. Commit `aa3c5e2` corrected the response after its stated range became stale during the same handoff rotation.

### P2-6: The sealed gallery conflicts with the four-dungeon contract

Status: fixed in `0fbfa82`.

File: `docs/decisions.md:91,314,397,702`, `docs/world/places.md:15-27,72-74`, and `docs/roadmaps/phase-4-region-one.md:11-15,86-96`.

Trigger: Count the places that the current documents classify as dungeons after D-562 adds the sealed gallery.

Expected: The region-one scope and the content contracts use one classification. A later decision that changes the count or meaning of D-56, D-244, or D-313 must record the revision in each affected row.

Actual: D-56, D-244, and D-313 define exactly four dungeons. D-562 calls the sealed gallery a dungeon map, and `docs/world/places.md` lists it with kind `Dungeon`, in addition to the four named dungeons. D-562 records no revision of the three earlier decisions. The order-of-play paragraph still says that the party plays four dungeons, then sends the party through the gallery.

Consequence: PR-81 has no stable classification. An implementer cannot tell whether it builds a fifth dungeon with the common dungeon contract or a route that only uses the dungeon-map format. The region-one scope and place count also disagree.

Correction: Ask the owner to classify the sealed gallery. If it is a fifth dungeon, revise D-56, D-244, and D-313 and update the counts. If it is a route that uses the dungeon-map format, state that distinction in D-562, the places table, and the Phase 4 scope.

Regression check: The decision register, the places table, the order-of-play paragraph, and the PR-81 scope give one count and one classification for the sealed gallery. Every revised decision row names the later decision.

Second repeat review: D-575 classifies the gallery as the fifth dungeon and revises D-56, D-244, D-313, D-562, and D-564. D-327, D-346, and D-369 also name the changed count. The design, world files, Phase 3, Phase 4, and the affected area files agree on five dungeons and the order of play. OQ-180 assigns the unresolved boss choice to PR-81, as D-487 requires.

### P2-7: PR-81 does not carry the complete art and effect contract

Status: fixed in `ff04f87`.

File: `docs/roadmaps/phase-4-region-one.md:108-115`, `docs/roadmaps/area-art.md:176-186`, and `docs/roadmaps/area-effects.md:286-294`.

Trigger: Use the area ownership tables and the PR-81 exit tests to implement the full dungeon contract of D-575.

Expected: PR-81 owns the art and effects of the sealed gallery, and its budget test covers each map and battle place (D-523, D-575). The contract must match the other Phase 4 dungeon builds.

Actual: The art and effects tables assign later-place work only to PR-23 through PR-27. The PR-81 exit test checks each map but omits its boss battle place. The equivalent exit test for PR-23 through PR-26 checks each map and battle place.

Consequence: A session can follow the area tables and leave the gallery art or effects unowned. PR-81 can also satisfy its stated exit tests without checking the effect budget of its boss battle.

Correction: Add PR-81 to the later-place rows of `area-art.md` and `area-effects.md`. Make the PR-81 budget exit test cover each map and each battle place.

Regression check: The art and effects ownership tables name PR-81, and its budget exit test uses the same map-and-battle-place boundary as the other dungeon builds.

Third repeat review: The art and effects ownership rows now name PR-81, the sealed gallery, and D-575. The PR-81 budget exit test now covers each map and each battle place under D-523. The adjacent scan found and corrected the PR-35 and PR-17 scope limits, which now include PR-81 among the other region-one nodes and places.

## Out of scope

- OQ-179 holds the JSON-reader reflection switch for PR-5. This review did not decide its implementation.
- The future code, content, art, and game behavior of PR-1 through PR-81 remain with their roadmap entries. This review checked their contracts and order, not implementations that do not exist.

## PR comments

- The first automated pass reported `Approved` with no issue. The initial review verified its issue comment and the absence of inline threads.
- The repeat pass on `d31ae6b` found that the response named Session 28 instead of Session 29 as the archive head. The author accepted the finding, corrected both lines in `aa3c5e2`, replied with the regression result, and resolved the thread. The final pass on `c8e7e48` reports `Approved` with one closed finding and no open finding. The repeat review verified the trigger, the correction, and the resolved thread.
- The pass on `6a43f1a` found that the Phase 4 summary still counted four new dungeons. The author accepted the finding and corrected it in `0fbfa82`. The final pass reports `Approved` with two closed findings and no open finding. This review verified the old text, the correction, and the wider count scan.
- The pass on `ff04f87` found that the response and handoff counted three changed roadmap files instead of four. The author accepted the finding and corrected the three stale lines in `54edbe5`. The final pass on `ef5a8b0` confirms the correction, with three closed findings and no open finding. This review verified the correction and the resolved thread.

## Description edits

- PR title: changed the decision range from `D-555 to D-572` to `D-555 to D-574`.
- Checks section: changed the gap-free decision range from `D-1 to D-572` to `D-1 to D-574`.
- PR title: changed the decision range from `D-555 to D-574` to `D-555 to D-575`.
- PR gate: changed the automated-pass line from incomplete to complete after the approval on `0fbfa82`.
- PR description: changed the gap-free ranges from D-572 and OQ-179 to D-575 and OQ-180.
- PR gate: changed the other-provider review line from incomplete to complete after this verdict became current.

## Verification

- `git fetch origin`: completed before the repeat review.
- `gh pr view 12 --json ...`: PR #12 is open, targets `main`, and has branch tip `74f94772e03bd553978ceeefa689ed0d87c4796c` before this review metadata.
- `git merge-base origin/main HEAD`: `c4d39fe70dfd6199740ba4948e183d66fe0ce6b9`.
- First repeat-review effective-head scan: `d31ae6b3e1b5e786e4ad2f24bbbf6ee969085866` was the newest substantive commit. The later commits changed only review and handoff metadata.
- Effective-head scan by exact metadata paths: `0fbfa820e56746209923c557a94c3b7519cffc91` is the newest substantive commit. The later commit changes only `docs/session-handoff.md`.
- Third repeat-review effective-head scan by exact metadata paths: `ff04f87824ae1cfae947fd05ec20f5afcf64a67b` is the newest substantive commit. The later commits change only `docs/reviews/pr-12-response.md` and `docs/session-handoff.md`.
- Correction diff: inspected every changed path from the prior review tip `03c963a` through `d31ae6b`, plus metadata through `c8e7e48`.
- Original findings: reproduced each trigger and regression check. P1-1 and P2-1 through P2-5 are fixed as recorded above.
- New PR-27 scope: D-574, sections 7 and 8 of `docs/design.md`, the Phase 4 sequence, `area-exploration.md`, `docs/world/places.md`, and `docs/world/arc.md` agree on PR-23, PR-24, PR-81, PR-27, PR-25, and PR-26.
- Adjacent classification check: found the unresolved four-dungeon conflict in P2-6.
- P2-6 correction diff: inspected every changed path from `d31ae6b` through `0fbfa82`, plus metadata through `74f9477`.
- P2-6 regression: the live-document count scan gives five dungeons, one classification for the gallery, and a D-575 mark on each old count.
- Adjacent full-contract check: found the missing PR-81 ownership rows and battle-place budget test in P2-7.
- P2-7 correction diff: inspected every changed path from `0fbfa82` through `ff04f87`, plus metadata through `ef5a8b0`.
- P2-7 regression: both ownership tables name PR-81 and D-575, and the PR-81 budget test covers each map and each battle place under D-523.
- Adjacent group-scope check: the PR-35 and PR-17 exclusions include PR-81. The four remaining references to PR-23 through PR-26 or PR-27 without PR-81 are a historical finding, the heading of the four-PR entry, and the exploration table that gives PR-81 its own row.
- Automated pass: all three findings had merit. Commits `aa3c5e2`, `0fbfa82`, and `54edbe5` corrected them, and the final pass reports approval with no open finding.
- Archive check: headings run from Session 34 through Session 1 with no gap or repeat. Session 34 moved word for word from the handoff.
- First repeat-review register check: D-1 through D-574 and OQ-1 through OQ-179 were gap-free.
- Register check: D-1 through D-575 and OQ-1 through OQ-180 are gap-free.
- `git diff --check c4d39fe70dfd6199740ba4948e183d66fe0ce6b9..d31ae6b3e1b5e786e4ad2f24bbbf6ee969085866`: passed.
- `git diff --check 84b4128eb67462daf0e272e3aef9fc102307a118..d31ae6b3e1b5e786e4ad2f24bbbf6ee969085866`: passed.
- `git diff --check d31ae6b3e1b5e786e4ad2f24bbbf6ee969085866..0fbfa820e56746209923c557a94c3b7519cffc91`: passed.
- `git diff --check c4d39fe70dfd6199740ba4948e183d66fe0ce6b9..0fbfa820e56746209923c557a94c3b7519cffc91`: passed.
- `git diff --check 0fbfa820e56746209923c557a94c3b7519cffc91..ff04f87824ae1cfae947fd05ec20f5afcf64a67b`: passed.
- `git diff --check c4d39fe70dfd6199740ba4948e183d66fe0ce6b9..ff04f87824ae1cfae947fd05ec20f5afcf64a67b`: passed.
- `python3 docs/tools/ste-check.py $(git ls-files '*.md' | grep -v -e '^docs/reviews/' -e '^docs/session-handoff' -e '^docs/archive/')`: 0 findings.
- `cmp -s AGENTS.md CLAUDE.md`: passed.
- Build, test, format, smoke, determinism, replay, bot, night, and screen checks did not run because the repository has no code or solution yet. Their roadmap rows name the PR that creates each check.
- `gh pr checks 12`: no checks are reported on the branch. The automated-pass evidence is its review comment and resolved thread.
- Push: `34c1cf33a713794583c28f9de9d5cf591ab8551a` is published on `origin/docs/pr-12-critic` and verified with `gh pr view`.

## Open questions and accepted risks

- D-573 resolves OQ-105 and fixes P1-1.
- OQ-179 blocks PR-5 and remains assigned to that PR.
- D-575 resolves P2-6. OQ-180 blocks PR-81 and does not block this PR.
- No accepted risk resolves the open finding.

## Earlier verdicts

- 2026-09-16, head `84b4128eb67462daf0e272e3aef9fc102307a118`: Changes required. P1-1 and P2-1 through P2-5 were open.
- 2026-09-16, head `d31ae6b3e1b5e786e4ad2f24bbbf6ee969085866`: Changes required. P2-6 was open.
- 2026-09-16, head `0fbfa820e56746209923c557a94c3b7519cffc91`: Changes required. P2-7 was open.

## Verdict

**Ready for owner merge.** This verdict applies to head `ff04f87824ae1cfae947fd05ec20f5afcf64a67b`. P2-7 is fixed, the adjacent scope scan passes, the automated pass has no open finding, and no unresolved review thread remains.
