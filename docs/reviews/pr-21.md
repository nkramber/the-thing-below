# PR-21 review

Date: 2026-09-17

## Identity

- PR: 21
- Target: `main`
- Base: `04953e40941d5df84aa436d0fc192b75e092ea57`
- Merge base: `04953e40941d5df84aa436d0fc192b75e092ea57`
- Head: `40ea2756031f1ddf59e499bb18d4d29daaa3f6a7`
- Branch: `feat/pr-3-review-gate`

## Provider gate

The author provider is Claude Code, as Session 78 states in `docs/session-handoff.md`. The active reviewer provider is Codex. The providers differ, so this review meets T-4 and D-17.

## Intended behavior and scope

PR-3 adds the `review-gate` command and its workflow. The review inspected all 28 changed paths: `.claude/skills/gitar-review/SKILL.md`, `.claude/skills/one-pr-one-session/SKILL.md`, `.claude/skills/pr-review/references/review-record.md`, `.github/pull_request_template.md`, `.github/workflows/review-gate.yml`, `AGENTS.md`, `CLAUDE.md`, `TheThingBelow.Tests/ReviewGateCommandTests.cs`, `TheThingBelow.Tests/ReviewGateFixture.cs`, `TheThingBelow.Tests/ReviewGateRuleTests.cs`, `TheThingBelow.Tests/ToolsCommandLineTests.cs`, `TheThingBelow.Tools/Program.cs`, `TheThingBelow.Tools/ReviewGate/DocumentRules.cs`, `TheThingBelow.Tools/ReviewGate/EffectiveHead.cs`, `TheThingBelow.Tools/ReviewGate/GateCheck.cs`, `TheThingBelow.Tools/ReviewGate/MarkdownSection.cs`, `TheThingBelow.Tools/ReviewGate/OverrideRules.cs`, `TheThingBelow.Tools/ReviewGate/PullRequestFacts.cs`, `TheThingBelow.Tools/ReviewGate/ReviewGateCommand.cs`, `TheThingBelow.Tools/ReviewGate/ReviewRecordRules.cs`, `docs/decisions.md`, `docs/design.md`, `docs/questions.md`, `docs/roadmaps/area-ci.md`, `docs/roadmaps/area-tools.md`, `docs/roadmaps/phase-1-foundations.md`, `docs/session-handoff-archive.md`, and `docs/session-handoff.md`.

The relevant contracts are T-2, T-3, T-4, T-6, D-15, D-17, D-18, D-22, D-67, D-401, D-500, D-560, D-577, D-578, D-579, D-581, D-609, and D-610. The review traced the workflow from the `pull_request_target` event through fact collection and the eight command rules. No changed area remains uninspected.

## Findings

### P1-1: The verdict check accepts a negated approval

Status: open.

File: `TheThingBelow.Tools/ReviewGate/ReviewRecordRules.cs:84-90`.

Trigger: Put `**Not Ready for owner merge.**` in the `## Verdict` section.

Expected: RG 4 accepts only the exact current verdict `Ready for owner merge` (D-15, D-17).

Actual: `CheckVerdict` searches for the approved phrase as a substring. It finds one verdict name and passes, even though the text negates approval.

Consequence: A record that explicitly refuses merge can satisfy the review gate and allow a merge without an approval.

Correction: Parse the verdict line in the section and require the exact approved value.

Regression check: Add a test for `Not Ready for owner merge`; RG 4 must fault.

### P1-2: The head check accepts a field outside Identity

Status: open.

File: `TheThingBelow.Tools/ReviewGate/ReviewRecordRules.cs:135-147`.

Trigger: Give `## Identity` a stale or absent head field, then add a matching `- Head:` line under another section.

Expected: RG 5 reads the head field from the Identity list alone (D-17, D-610).

Actual: `CheckHead` scans every line of the record and accepts the first matching field, wherever it appears.

Consequence: A stale or malformed review identity can pass RG 5 when unrelated text names the effective hash.

Correction: Read the `## Identity` section and require its head field there.

Regression check: Add a test with a stale Identity head and a matching head field in another section; RG 5 must fault.

### P2-1: The deferral check misses spelled-out pull requests

Status: open.

File: `TheThingBelow.Tools/ReviewGate/DocumentRules.cs:52-70,210-219`.

Trigger: A Documents line says a required record will be added by “a separate pull request.”

Expected: RG 8 faults when a Documents line defers a document or record of this PR to a later PR (D-577, D-578, D-579).

Actual: The phrase list contains “a separate pr” but not “a separate pull request,” so the line passes RG 8.

Consequence: A required document or record can be left for later while the machine check reports no deferral.

Correction: Recognize spelled-out variants of the deferral phrases, including “separate pull request.”

Regression check: Add a test for “a separate pull request”; RG 8 must fault.

## Out of scope

- The night gate belongs to PR-49, and det-lint belongs to PR-46, as the PR-3 roadmap states.
- The screen test belongs to PR-41, replay identity belongs to PR-4, and bot tests belong to PR-15.

## PR comments

- Gitar approved effective head `40ea275` and reported no finding. Its dashboard comment is current after the push. I verified the described changes and found the findings above; functional validation was not enabled in the Gitar pass.

## Description edits

- Replaced ``- `docs/reviews/`: No change needed because `docs/reviews/` takes the record of the other provider, and this PR waits for that review.`` with ``- `docs/reviews/`: Changed: `docs/reviews/pr-21.md`. The review record lists the findings for effective head `40ea275`.`` The review record now exists on the PR branch.

## Verification

- `git fetch origin`, `gh pr view 21`, and `git merge-base`: base and merge base are `04953e4`; PR tip is `d5d9332`; the newest non-metadata commit is effective head `40ea275`.
- `gh pr checks 21`: all nine reported CI checks pass. `gh run view 35257480061` confirms the completed successful run targets PR tip `75b5f6320ffcf093d06dec3437a28f943ee9ecde`.
- `make verify`: pass on the local checkout, with 113 tests, clean format, 0 STE findings, and a successful Godot smoke session.
- `git diff --check 04953e4 40ea275`: pass.
- `diff -q CLAUDE.md AGENTS.md`: pass; the files are identical.
- Live `review-gate` check: not available on PR-3 because GitHub starts `pull_request_target` from `main` alone (F-37, D-500). The command tests provide the required PR-local proof.
- Checks not yet created: `det-lint` (PR-46), `replay-identity` (PR-4), `screen-test` (PR-41), bot (PR-15), and `night-gate` (PR-49), as the PR description records under G-16.
- Push: `1bdaa894447f938f522185b76578dc8bb1ca11af` is the head of `origin/feat/pr-3-review-gate`, verified with `gh pr view`.

## Open questions and accepted risks

- OQ-3 remains open. D-4 and D-15 require the owner to require the new check after its first live run.
- The review-gate check cannot run on this PR. D-500 accepts the test fixtures as PR-3 evidence, and the first live run is on the next PR.

## Verdict

**Changes required.** This verdict applies to head `40ea2756031f1ddf59e499bb18d4d29daaa3f6a7`. RG 4 can accept a negated approval, RG 5 can accept a head field outside Identity, and RG 8 misses spelled-out deferrals.
