# PR-36 review

Date: 2026-09-19

## Identity

- PR: 36
- Target: `main`
- Base: `122f3efe351c0e146cde5b4b7c3b3683c6aec071`
- Merge base: `122f3efe351c0e146cde5b4b7c3b3683c6aec071`
- Head: `f513ff3e86fa32a63e4cc936483dfcdf371d001d`
- Branch: `docs/pr-88-refute-f-84`

## Provider gate

Claude Code authored the substantive changes, as shown by Session 130 and the PR handoff. Codex is the reviewer in this session. The provider gate passes under T-4 and D-17.

## Intended behavior and scope

The PR corrects F-84 and supersedes D-673 with verified evidence. It records D-680, D-681, the open owner action of OQ-3, and the related roadmap and handoff updates. The review inspected the complete diff from `122f3ef` to `ae1ff1f` across all six changed paths: `docs/decisions.md`, `docs/design.md`, `docs/questions.md`, `docs/roadmaps/phase-1-foundations.md`, `docs/session-handoff.md`, and `docs/session-handoff-archive.md`. The effective implementation head is `f513ff3` because `ae1ff1f` changes only the metadata handoff. The affected contracts are decision supersession, documented finding status, open-question continuity, roadmap scope, session continuity, and the Documents section. No changed area remains uninspected.

## Findings

No finding.

## Out of scope

None.

## PR comments

- The author reported that the automated pass found a valid Documents-section fault and that the row was corrected. The review verified the current description uses the required `Changed:` form, and run `35459166100` reports RG 7 pass. The same comment reports RG 3 fault because this review record is not yet on the head. The review verified that fault at run `35459272689` and treats it as resolved by this record.
- Gitar approved the effective head with no code finding. The review accepts the approval as current for `f513ff3`.

## Description edits

None.

## Verification

- `git fetch origin`: passed. The merge base is `122f3efe351c0e146cde5b4b7c3b3683c6aec071`.
- `git diff --stat 122f3ef ae1ff1f` and the complete path diff: passed. All six changed paths were inspected.
- `git log --follow -- docs/reviews/pr-33.md`: passed. It gives squash commit `3204545`.
- `git show 3204545:docs/reviews/pr-33.md`: passed. It names Claude Code as author, Codex as reviewer, and the verdict for `d1b2305`.
- `git log -- TheThingBelow.Tools/ReviewGate/ReviewRecordRules.cs`: passed. The absent-file rule has one commit, `9787b2d`.
- `ReviewRecordRules.cs` at `9787b2d`: passed. RG 3 returns a fault for an absent review record.
- `gh run view 35459272689 --log-failed`: passed as negative evidence. RG 3 faults because `docs/reviews/pr-36.md` is absent, while RG 1, RG 2, RG 6, RG 7, and RG 8 pass.
- `make verify`: passed locally with 794 tests, 0 failures, 0 warnings, format clean, det-lint clean, STE check clean, replay identity clean, content hash clean, and a successful bounded Godot smoke session.
- Push: `d14225b` is the head of `origin/docs/pr-88-refute-f-84`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-3 remains open until the owner enables branch protection on `main`, as recorded by D-681. It blocks the enforced Gate 1 condition, not this review verdict.

## Verdict

**Ready for owner merge.** This verdict applies to head `f513ff3e86fa32a63e4cc936483dfcdf371d001d`. The provider gate, complete diff review, historical evidence, current PR comments, focused checks, and local project gates pass, with no blocking finding.
