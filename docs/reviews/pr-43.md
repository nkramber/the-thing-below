# PR-43 review

Date: 2026-09-20

## Identity

- PR: 43
- Target: `main`
- Base: `ea2fec5`
- Merge base: `ea2fec5`
- Head: `fd9e0ae`
- Branch: `feat/pr-45-debug-assembly`

## Provider gate

The PR handoff identifies Claude Code as the author provider. The active reviewer is Codex. The providers are opposite, so the gate passes under T-4 and D-17.

## Intended behavior and scope

The PR adds the development-only debug assembly, the four-command console, the Game reflection seam, the release-export exclusion, and the related tests and documentation. The review traced command input, intent queuing, tick application, replay, release behavior, console focus, assembly loading errors, and export workflow checks.

Inspected every path in `git diff --stat ea2fec5..fd9e0ae`: `.claude/skills/csharp-conventions/SKILL.md`, `.github/workflows/export.yml`, all files under `TheThingBelow.Debug/`, the changed Game, Tests, Tools, solution, docs, roadmap, runbook, and handoff paths. No changed path remains uninspected.

The affected contracts are the Core boundary, determinism, replay, errors, input and CI boundaries, presentation, dependencies, and the PR-45 roadmap exit tests. No new Core dependency or simulation-version change is required.

## Findings

No finding.

## Out of scope

- Later debug features behind the same seam, including capture PR-74 and sound room PR-71, remain outside PR-45 scope.
- The screen-test job belongs to roadmap PR-41 and is not an exit test of this PR.

## PR comments

- Gitar reported one stale duplicate `DescribeConsole` comment at `09d87adf`. The author removed it in `fd9e0ae`, and the current file retains only the seam-based explanation. The review verified the correction and the closed thread.
- The Gitar dashboard approved `fd9e0ae`. Its separate review-gate failure was the expected missing review record, not a code finding.

## Description edits

None.

## Verification

- `git fetch origin`: passed. Base is `ea2fec5`.
- Complete diff inspection and `git diff --check ea2fec5..fd9e0ae`: passed.
- `make verify`: passed. Build, 1,213 tests, format, det-lint, STE, replay identity, content hash, atlas, and smoke all passed.
- GitHub build, test, format, smoke, replay identity, det-lint, STE, coverage, changed-path, and export checks: passed for `fd9e0ae` on all applicable legs.
- Gitar: passed for `fd9e0ae`; its one finding is closed.
- GitHub review-gate: expected failure before this record, because RG 3 cannot pass without this file.
- `git diff --name-only fd9e0ae..99c8c5c`: metadata only, so `fd9e0ae` remains the effective head.
- Push: pending. The review record and handoff entry must be committed and pushed before this line can be finalized.

## Open questions and accepted risks

None. OQ-115 does not affect PR-43.

## Verdict

**Ready for owner merge.** This verdict applies to head `fd9e0ae`. The provider gate, complete diff review, roadmap exit tests, focused behavior checks, local gates, and current CI evidence pass, with no blocking finding.
