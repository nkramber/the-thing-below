# PR-82 review

Date: 2026-09-25

## Identity

- PR: 82
- Target: `main`
- Base: `640ad98ede37ab6123f6697d314b6333a507436a`
- Merge base: `640ad98ede37ab6123f6697d314b6333a507436a`
- Head: `e17c964e35c7dedad9a9cdede543276595f91d4b`
- Branch: `fix/pr-105-save-drift`

## Provider gate

The author provider is Anthropic. Sessions 311 and 312 name Claude Code as the author, and the PR description binds the author work to this PR. The active reviewer is Codex from OpenAI. The providers differ, so the review meets T-4 and D-17.

## Intended behavior and scope

PR-105 applies nine assigned repository-review findings under D-1109. It changes save drift, wipe selection, save recording, CI package verification, determinism lint, map drawing checks, battle guards, and remap conflict feedback. I inspected the complete diff from merge base `640ad98` to effective head `e17c964`.

All 78 paths in `git diff --stat 640ad98...e17c964` were inspected. The 74 paths from the prior review remain as listed above. The four additional paths are `TheThingBelow.Game/scripts/CaptureSession.cs`, `TheThingBelow.Game/scripts/ScreenCaptures.cs`, `docs/reviews/pr-82-response.md`, and this review record, `docs/reviews/pr-82.md`.

The review traced save headers through `GameRun.Reload`, Core resume, story snapshots, patrol reconstruction, and the run log. Same-build saves retain strict checks; other-build saves adjust map and scene data with warning entries. Save formats 14 and 4 carry story step ids, and the simulation version and identity file change. The wipe pick filters by seed. The workflow verifies the ReportGenerator package digest before install. The lint additions cover disabled code, file and OS access, multiline scene text, and binary scenes. Drawing checks read all content maps and tile kinds. The round 3 correction adds the cursor outline and a two-conflict capture. No changed path remains uninspected.

## Findings

### P2-1: The selected conflicting binding loses its warning color

Status: fixed in `af1d924dad50e9e8c57c87a26d05cb9cc5d77e07`.

Open at: `0e71601a8ef762a9a5e0032e8346c682f0e06c18`.

File: `TheThingBelow.Game/scripts/Ui/SettingsScreen.cs:443-449`.

Trigger: A remap conflict includes the binding cell whose row is currently selected.

Expected: Every cell whose binding sits in a conflict uses the warning color (D-1119, F-146).

Actual: `ShowSlot` first paints the selected cell with `chosenColor`, then skips the warning override when `chosen` is true. The `settings-conflict-1x` artifact at this head shows the selected Back cell in yellow while the conflicting Confirm cell is red.

Consequence: The selected conflicting binding does not carry the warning color, so the grid does not mark every conflict as D-1119 requires. The baseline accepts this mismatch, and the new tests check `InConflict` but do not check the rendered color of a selected conflict.

Correction: `ShowSlot` now uses the warning color for each conflict cell. The selected conflict cell also uses a 2-pixel cursor-color outline (D-1120).

Regression check: `SettingsMenuTests.EachCellOfAConflictTakesTheWarningLookUnderTheCursorToo` passes for all four cell looks. CI `screen-test` compares the two-conflict capture twice against the new baseline. The artifact shows both conflicts in red, the selected cell with its yellow outline, and the line `1 of 2`. `make verify` passes at `e17c964`.

## Out of scope

P3-9, P3-24, P3-25, P3-26, P3-36, and P3-37 are listed outside PR-105 in section 7.41 of `docs/roadmaps/phase-2-first-playable.md`. That roadmap section does not assign those items to another PR.

## PR comments

- The Gitar CI analysis on `af1d924` claimed two `review-gate` faults. The author answered that claim at 18:53 UTC. Job `108204793906` confirms RG 4 and RG 5 alone fault because the prior verdict and head were stale; RG 1 to RG 3 and RG 6 to RG 8 pass. This record replaces the stale verdict and head. The earlier RG 3 claims for missing review records were answered at 17:34 and 17:48 UTC, as recorded in the prior review. The current clean approval has no item and needs no answer (D-964).
- No Gitar review thread exists.

## Description edits

- Changed the `docs/reviews/` Documents row from `No change needed because` the review comes next to `Changed: docs/reviews/pr-82.md` because this record now exists.
- Replaced the stale `Screens (D-784)` paragraph about the one-conflict frame from `1cd661d` and baseline `0e71601` with the verified two-conflict frame from `af1d924` and baseline commit `e17c964`.
- Read back the updated `Screens (D-784)` paragraph after `gh pr edit`.

## Verification

- `git fetch origin`, `git status --short --branch`, `git merge-base origin/main HEAD`, and `git rev-parse HEAD`: clean `review/pr-82` checkout; base and merge base `640ad98`; effective head `e17c964`, equal to the PR head.
- `make verify` at `e17c964` on macOS arm64: passed. Build had 0 warnings and 0 errors; 3401 tests passed; format, det-lint (0 findings), STE (0 findings), replay identity (12 runs), content hash, atlas, and smoke passed.
- CI run `36175444520` at `e17c964`: build, test, and format passed on macOS 26, Ubuntu 24.04, and Windows 2025. Coverage, det-lint, replay identity, smoke, STE, and `screen-test` passed. I read `settings-conflict-1x.png` from the screen-test artifact.
- CI `review-gate`, run `36175443655`, reports RG 1 to RG 3 and RG 6 to RG 8 passed. RG 4 and RG 5 fail because the existing record names the prior verdict and head. The Gitar analysis claim has the author answer recorded above.
- The current Gitar dashboard edited at 18:51 UTC after the head push; it approves and lists no code finding. No review thread exists. D-964 means its clean approval needs no answer.
- `gh pr edit 82 --body-file /tmp/pr-82-body-updated.md` replaced the stale screen paragraph. `gh pr view` confirms the description now states the two-conflict frame and current baseline.
- Bot and night-gate checks do not yet exist; PR-15 and PR-49 create them, as section 7.41 records.
- Push: this metadata commit, which contains the updated review record and handoff, is the head of `origin/fix/pr-105-save-drift`, verified with `gh pr view` after publication.

## Open questions and accepted risks

No open question affects this PR. Saves before format 14 contain no story step id and resume by the stored index, as D-1112 directs.

## Earlier verdicts

Session 313 gave Changes required for head `0e71601a8ef762a9a5e0032e8346c682f0e06c18`, because the selected conflicting cell did not use the warning color.

## Verdict

**Ready for owner merge.** This verdict applies to head `e17c964e35c7dedad9a9cdede543276595f91d4b`. P2-1 is fixed, its regression check passes, and the current implementation checks pass. The review-gate failures read the prior record and should pass when this metadata record reaches the PR.
