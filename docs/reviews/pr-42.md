# PR-42 review

Date: 2026-09-20

## Identity

- PR: 42
- Target: `main`
- Base: `2a8115b`
- Merge base: `2a8115b`
- Head: `2ead9c8`
- Branch: `feat/pr-7-tile-map`

## Provider gate

The handoff entries of PR-42 identify Claude Code as the author provider. This review is by Codex, the opposite provider. The provider gate passes under T-4 and D-17.

## Intended behavior and scope

The PR adds the fixture tile map, four-direction movement, party and patrol sight, walked-tile persistence, save format 2 migration, the map scene, the camera, held-step input, and the first tile content. The review inspected all 83 paths in `git diff --name-only 2a8115b..2ead9c8`, including Core, Game, Storage callers, Tests, content, identity files, and the affected design, decision, question, roadmap, and handoff documents. The applicable contracts are Core boundaries, determinism, replay, errors, content, strings, input, gameplay, presentation, and atlas validation. No area remains uninspected.

## Findings

No finding.

## Out of scope

- The map HUD belongs to PR-64 under D-721.
- Enemies and patrol movement belong to PR-8.
- Dungeon interactions belong to PR-16 and PR-64.
- Screen capture tests belong to PR-41 under F-26 and G-16.

## PR comments

- Gitar reported that opening the menu while a direction was held could queue both intents and crash. The author fixed the trigger at `2ead9c8` with `GameRun.MenuOpenNextTick`, and the review verified the queue ordering and regression tests. The current Gitar pass approves the reviewed head and has one closed finding.
- Gitar reported the review-gate fault caused by the absent review record. The fault is the expected pre-review RG 3 state, and the record in this commit resolves it.

## Description edits

None.

## Verification

- `git fetch origin`: passed. Base `2a8115b`, effective head `2ead9c8`, metadata tip `e4b311f`.
- `gh pr view 42 --json ...`: passed. Target, branch, commits, comments, current Gitar approval, and check results match the review target.
- `git diff --stat 2a8115b..2ead9c8` and staged path reads: passed. All 83 changed paths were inspected.
- `make verify` at `2ead9c8`: passed. Build passed with 0 warnings and 0 errors. Tests passed with 1,182 tests. Format, det-lint, STE, replay identity, content hash, atlas, and smoke passed.
- CI at the PR tip: build, test, and format passed on Ubuntu, Windows, and macOS. Replay identity, smoke, coverage, det-lint, STE, changed paths, and Gitar passed. The only failed check is the expected pre-record `review-gate` RG 3 fault.
- Focused contract review: passed. Map boundaries, movement chaining, sight symmetry and range, walked-tile retention, save migration, state hashing, input event ordering, camera limits and centering, tile construction, and failure checks agree with the PR-7 scope and decisions D-715 to D-722.
- Push: `6b4cad0` is the head of origin/`feat/pr-7-tile-map`, verified with `gh pr view`.

## Open questions and accepted risks

None for PR-42. OQ-115 remains open for PR-8 and does not affect this PR.

## Verdict

**Ready for owner merge.** This verdict applies to head `2ead9c8`. The opposite-provider gate passes, the complete changed scope has review coverage, the PR-7 exit tests pass, and no blocking finding remains.
