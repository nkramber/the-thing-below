# PR-77 review

Date: 2026-09-24

## Earlier verdicts

- `Changes required` for head `87cf4cc`. Finding P2-1 showed that command E could accept a Gitar check that completed after the 15-minute deadline.

## Identity

- PR: 77
- Target: `main`
- Base: `58dadb5`
- Merge base: `58dadb5`
- Head: `066901e`
- Branch: `docs/pr-100-gitar-resume`

## Provider gate

The author provider is Claude Code. Session 280 of `docs/session-handoff.md` records Claude Code as the PR-100 author. The reviewer provider is Codex. The providers differ, so the review passes T-4 and D-17.

## Intended behavior and scope

PR-100 ends the Gitar pause and polls the Gitar check after each push. The first review inspected all 15 paths in the base diff. This repeat review inspected the full diff from `87cf4cc` to `066901e`, including the response, review record, handoff files, and the changed Gitar poll. No area remains uninspected.

The base diff paths are `.claude/skills/gitar-review/SKILL.md`, `.claude/skills/one-pr-one-session/SKILL.md`, `.claude/skills/pr-review/SKILL.md`, `.claude/skills/pr-review/references/answer-review.md`, `.github/pull_request_template.md`, `AGENTS.md`, `CLAUDE.md`, `docs/decisions.md`, `docs/design.md`, `docs/reviews/pr-77-response.md`, `docs/reviews/pr-77.md`, `docs/roadmaps/area-ci.md`, `docs/roadmaps/phase-2-first-playable.md`, `docs/runbooks/merge.md`, `docs/runbooks/session-context.md`, `docs/session-handoff-archive.md`, and `docs/session-handoff.md` (D-589).

The review checked D-1074's timeout, D-1073 and D-964's Gitar rules, the document and handoff rules D-577, D-581, and D-18, and the PR-100 exit tests. It also checked D-945's supersession by D-1073 and the response to P2-1.

## Findings

### P2-1: The poll accepts a check that completes after the deadline

Status: fixed in `066901e`.

Open at: `87cf4cc`.

File: `.claude/skills/gitar-review/SKILL.md:219-228`.

Trigger: The check remains in progress at 15 minutes, completes after that deadline, and the next poll reads it.

Expected: D-1074 requires the session to stop and tell the owner when the check does not complete within 15 minutes.

Actual: The old command accepted any `completed` status without its completion time.

Consequence: The old command could accept a late Gitar check and continue the review loop.

Correction: Command E reads `completed_at` and accepts a check only when it completed within 900 seconds of the push.

Regression check: The author reports results for completion ages of 100 and 1000 seconds, plus the no-check path. The expected outcomes are `completed`, `not complete`, and `no check`. I also checked that `fromdateiso8601` returns 900 and 901 for the deadline boundary timestamps.

## Out of scope

None.

## PR comments

- The CI analysis claim said `review-gate` failed at RG 3 and RG 7 because the review record did not exist. The author explained that the reviewer adds the record, and the CI log confirms that both faults had that cause. The current Gitar dashboard approves the current head and has no finding. The clean approval has no item and needs no answer (D-964).

## Description edits

None.

## Verification

- `git fetch origin`; local head and PR head matched at `066901e` before review.
- Merge base: `58dadb5`; all 17 paths in the base diff were inspected during the first review, and all five paths changed since that review were inspected again.
- The author response reports command E results for completion ages of 100, 1000, and no check. The changed jq expression and deadline arithmetic were inspected.
- `jq` parsed the deadline boundary timestamps as 900 and 901 seconds. The live Gitar check filter returned `completed success 1790291472`.
- CI run `36071384377`: build, test, format, smoke, det-lint, replay identity, screen-test, coverage, changed-paths, and STE passed. Matrix jobs skipped for this documentation change.
- Gitar check on `066901e` completed with success at `2026-09-24T23:11:12Z`. Its current dashboard review approves the head with no finding.
- CI run `36071381473`: `review-gate` failed only because the review record still gave the prior verdict. RG 3, RG 5, RG 6, RG 7, and RG 8 passed. A fresh review-gate result must read this record after publication.
- `make verify` was not run. The applicable CI checks passed, and PR-100 changes no code or screen.
- Push: pending.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** This verdict applies to head `066901e`. The earlier finding is fixed, and the current Gitar pass and applicable CI checks pass.
