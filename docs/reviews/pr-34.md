# PR-34 review

Date: 2026-09-19

## Identity

- PR: 34
- Target: `main`
- Base: `3204545`
- Merge base: `3204545`
- Head: `2434847`
- Branch: `docs/m1-m2-cost-model`

## Provider gate

The PR author is Claude Code, as recorded in Session 124 and the PR binding. The reviewer is Codex. The providers differ, so the gate passes under T-4 and D-17.

## Intended behavior and scope

The PR records M-1 and M-2 for the first ten code PRs, updates the phase order, records the owner decisions, and documents the measurement procedure. The review inspected the complete diff from `3204545` to `2434847`, the PR description, the existing comment export, the cost tables, the roadmap exit tests, the decision rows, and the runbook procedure.

Inspected paths:

- `docs/decisions.md`
- `docs/design.md`
- `docs/roadmaps/area-art.md`
- `docs/roadmaps/phase-1-foundations.md`
- `docs/runbooks/session-context.md`
- `docs/session-handoff.md`
- `docs/session-handoff-archive.md`

The review covered the documentation gate, decision references, measurement arithmetic, sequence order, and STE rules. No code, content, CI, or runtime behavior changed.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar reported that the first head described Windows as about twice Linux on every PR. The author changed the summary to two to three times and named the 2.4x mean. The review verified the corrected statement against the table, including the 3.00x PR-3 ratio. Gitar also reported the absent review record, which this record supplies.

## Description edits

None.

## Verification

- `git diff --stat 3204545..2434847`: seven documentation paths inspected.
- `python3` arithmetic probe: M-2 means are 91.1, 214.6, 116.3, 82.6, 504.6, and 133.2 seconds, which support the displayed rounded values. Windows/Linux ratios range from 2.04x to 3.00x, with a 2.4x mean by the table's rounded values.
- `make verify` at `2434847`: passed. Build had 0 warnings and 0 errors. The test run passed 782 tests with 0 failures and 0 skipped tests. `det-lint` reported 0 findings and named only the planned absent Audio and NormalMaps folders. Replay identity, content hash, and bounded smoke passed.
- `make ste-check` at `2434847`: passed with 0 findings.
- GitHub PR #34 at `1d9fe5b`: changed-paths and ste-check passed, Gitar passed, and review-gate failed only because this review record was absent before this review.
- Push: the review record and session handoff are on `origin/docs/m1-m2-cost-model`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-3 remains open for the owner. It does not block this review because it concerns whether the owner requires `review-gate` on `main`, not the correctness of this PR's documentation.

## Verdict

**Ready for owner merge.** This verdict applies to head `2434847`. The cross-provider gate, document gate, focused arithmetic checks, local verification, and corrected automated comment are complete, with no blocking finding.
