# PR-20 review

Date: 2026-09-17

## Identity

- PR: 20
- Target: `main`
- Base: `9b841588a2f77b8dc9420b822ee2b3ced297d160`
- Merge base: `9b841588a2f77b8dc9420b822ee2b3ced297d160`
- Head: `e800f4c4dcc635ca98a8ef7e16b67aad0423de59`
- Branch: `feat/pr-2-ste-checker`

## Provider gate

The author provider is Claude Code. Session 75 in `docs/session-handoff.md` names Claude Code as the author of PR #20. The reviewer provider is Codex. The providers differ, so the gate passes under T-4 and D-17.

## Intended behavior and scope

The PR adds the C# `ste-check` command, its writing, reference, and session checks, tests, and workflow integration. It retires the interim Python checker. I inspected the full diff from the merge base through `e800f4c`, the callers, the tests, the PR description, the roadmap entry, the relevant decisions, and every existing PR comment.

Inspected paths from `git diff --stat` (D-589): `.claude/skills/ste-writing/SKILL.md`, `.githooks/pre-commit`, `.github/pull_request_template.md`, `.github/workflows/ci.yml`, `AGENTS.md`, `CLAUDE.md`, `Makefile`, `TheThingBelow.Tests/SteCheckCheckout.cs`, `TheThingBelow.Tests/SteCheckCommandTests.cs`, `TheThingBelow.Tests/SteCheckFixtures.cs`, `TheThingBelow.Tests/SteCheckReferenceRuleTests.cs`, `TheThingBelow.Tests/SteCheckSessionNumberTests.cs`, `TheThingBelow.Tests/SteCheckWritingRuleTests.cs`, `TheThingBelow.Tests/ToolsCommandLineTests.cs`, `TheThingBelow.Tools/Program.cs`, `TheThingBelow.Tools/SteCheck/DocumentSet.cs`, `TheThingBelow.Tools/SteCheck/EnglishWords.cs`, `TheThingBelow.Tools/SteCheck/Finding.cs`, `TheThingBelow.Tools/SteCheck/IdRegister.cs`, `TheThingBelow.Tools/SteCheck/MarkdownText.cs`, `TheThingBelow.Tools/SteCheck/ReferenceRules.cs`, `TheThingBelow.Tools/SteCheck/SessionNumberRules.cs`, `TheThingBelow.Tools/SteCheck/SteCheckCommand.cs`, `TheThingBelow.Tools/SteCheck/WritingRules.cs`, `docs/decisions.md`, `docs/design.md`, `docs/questions.md`, `docs/roadmaps/area-ci.md`, `docs/roadmaps/area-tools.md`, `docs/roadmaps/phase-1-foundations.md`, `docs/runbooks/dev-machine.md`, `docs/runbooks/session-context.md`, `docs/session-handoff-archive.md`, `docs/session-handoff.md`, and `docs/tools/ste-check.py` (deleted). No changed path remains uninspected.

The affected contracts are T-1, T-2, T-3, T-5, T-6, D-10, D-18, D-20, D-101, D-277, D-604 to D-608, F-5, F-11, G-12, G-16, and L-12. Core determinism, replay, content, gameplay, and presentation do not apply because the PR changes no simulation, game content, player strings, or presentation behavior. CI changes only add the checker step and keep read-only contents permission and full-SHA action pins.

The PR-2 roadmap entry names the command, all three checker areas, the Python retirement, and the exit tests. The changed decisions and questions agree with the code. `AGENTS.md` and `CLAUDE.md` are identical. The PR description has a line for every Documents row, but its `docs/reviews/` line does not use one of the required D-581 forms. This review corrects that line below.

## Findings

No finding.

## Out of scope

None.

## PR comments

Gitar reported that `he's`, `she's`, and `who's` passed rule 4.2. The author added all three alternatives and seven tests in `e800f4c`. The reply states that the old pattern missed the three forms and that possessives have no apostrophe. The pattern now matches these contractions, and the tests cover them. The thread is resolved, and Gitar approved `e800f4c`.

## Description edits

- The `docs/reviews/` Documents line changed from `This PR waits for the Codex review of PR-2, which writes docs/reviews/pr-<number>.md.` to `Changed: docs/reviews/pr-20.md. This review records the result for PR-20.` The previous line did not use a form required by D-581.

## Verification

- `git diff --check 9b841588a2f77b8dc9420b822ee2b3ced297d160...e800f4c`: passed.
- `make verify` at `e800f4c`, macOS arm64: build passed with zero warnings and errors; 53 tests passed; format passed; STE check reported 0 findings; Godot smoke ended with no error.
- `gh pr checks 20`: all nine checks passed on PR tip `2e316e06ca5b305a09ce98cd4109ef21b76965e9`: three build, test, and format legs; three smoke legs; coverage report; STE check; changed paths.
- `git diff --stat 9b841588a2f77b8dc9420b822ee2b3ced297d160...e800f4c`: 35 paths inspected; the full diff was read in stages.
- `gh pr view 20` and the one-command comment export: base, branch, PR state, description, comments, and Gitar thread verified. The effective head is `e800f4c`; the later tip changes only metadata files.
- Push: `1b895e913769002cc603150ba49c8a3d63ba44bc` is the head of `origin/feat/pr-2-ste-checker`, verified with `gh pr view`.

## Open questions and accepted risks

No open question affects PR-2. The PR implements heuristic STE grammar checks as scoped in the skill and roadmap; no additional accepted risk blocks this review.

## Verdict

**Ready for owner merge.** This verdict applies to head `e800f4c4dcc635ca98a8ef7e16b67aad0423de59`. The opposite-provider gate passes, the full diff and required checks pass, and no blocking finding remains. The review record and review handoff are on the remote.
