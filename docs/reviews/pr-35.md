# PR-35 review

Date: 2026-09-19

## Identity

- PR: 35
- Target: `main`
- Base: `f896dc3`
- Merge base: `f896dc3`
- Head: `4e9338d`
- Branch: `fix/pr-87-content-hash-empty-option`

## Provider gate

Claude Code authored the substantive PR changes. The PR description and the top handoff entry identify Claude Code as the author. Codex reviews this PR. The providers differ, so the cross-provider gate passes under T-4 and D-17.

## Intended behavior and scope

The PR parses empty option values before a path or file reader receives them. Each affected command returns the fault exit code and names the option. The review inspected the command parsers, the shared option helper, the command-line tests, the decision row, the design finding, the Tools roadmap, the Phase 1 roadmap entry, and the session handoff files.

Inspected paths from the complete diff:

- `TheThingBelow.Tests/OptionValueTests.cs`
- `TheThingBelow.Tests/ToolsCommandLineTests.cs`
- `TheThingBelow.Tools/Atlas/AtlasCommand.cs`
- `TheThingBelow.Tools/Content/ContentHashCommand.cs`
- `TheThingBelow.Tools/DetLint/DetLintCommand.cs`
- `TheThingBelow.Tools/Identity/ReplayIdentityCommand.cs`
- `TheThingBelow.Tools/OptionValue.cs`
- `TheThingBelow.Tools/ReviewGate/ReviewGateCommand.cs`
- `TheThingBelow.Tools/SteCheck/SteCheckCommand.cs`
- `docs/decisions.md`
- `docs/design.md`
- `docs/roadmaps/area-tools.md`
- `docs/roadmaps/phase-1-foundations.md`
- `docs/session-handoff-archive.md`
- `docs/session-handoff.md`

No area remains uninspected.

## Findings

### P2-1: Scope text says six option values but lists seven

Status: open.

File: `docs/decisions.md:878`, `docs/design.md:343`, `docs/roadmaps/phase-1-foundations.md:810`, and the PR description.

Trigger: Read the scope statements and count the listed option values. They name `ste-check --root`, two `det-lint` options, `replay-identity --root`, `content-hash --root`, and two `review-gate` options, which totals seven.

Expected: The PR description, decision register, finding register, and roadmap must state one consistent scope and exit-test count (D-577, D-581, G-8).

Actual: The text repeatedly says “six option values” while listing seven. The implementation and regression theory cover nine command-option rows, including all seven non-`atlas` values and both `atlas` values.

Consequence: The recorded scope and exit tests do not describe the change accurately. A reader cannot determine whether one of the two `review-gate` options is intentionally excluded.

Correction: Replace the count “six” with “seven” where the seven non-`atlas` option values are listed, and state “nine command-option rows” where the regression theory includes both `atlas` options. Keep the implementation scope unchanged.

Regression check: Re-read the PR description and the four changed scope records, then run `make ste-check` and the review-gate command. The count must agree with the enumerated values and the theory rows.

## Out of scope

None.

## PR comments

- `gitar-bot` approved the code review and reported a review-gate failure because the review record was absent at that time. The review verified the pass is current for effective head `4e9338d`, reproduced all nine empty-option probes, and records the remaining scope-count defect above.

## Description edits

None.

## Verification

- `git fetch origin`: passed. The merge base is `f896dc3` and the effective implementation head is `4e9338d`. The later commit `c6e755b` changes only metadata paths.
- Complete diff from `f896dc3` to `c6e755b`: inspected all 15 changed paths listed above.
- `make verify` at `c6e755b`: passed with 794 tests, 0 failures, 0 warnings, 0 det-lint findings, 0 STE findings, matching replay identity and content hash, and a successful bounded smoke session.
- Focused empty-option probes at `c6e755b`: passed for all nine command-option rows. Each returned exit code 1, named the option, and produced no stack trace.
- `gh pr view 35 --json ...`: passed. The PR is open, targets `main`, and its head is `c6e755b`.
- `gitar-bot` comment export: passed. The current dashboard reports approval with no code finding. The review-gate failure is the expected missing-review-record state before this record is published.
- `review-gate`: pending this review record and handoff publication.
- Push: `c5a5346` is the head of `origin/fix/pr-87-content-hash-empty-option`, verified with `gh pr view`.

## Open questions and accepted risks

None for PR #35. OQ-3 remains an owner question for Gate 1 and does not affect this PR.

## Verdict

**Changes required.** This verdict applies to head `4e9338d`. The implementation and executable checks satisfy the empty-value behavior, but the PR records an inconsistent count for its enumerated scope and exit tests.
