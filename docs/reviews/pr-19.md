# PR-19 review

Date: 2026-09-17

## Identity

- PR: 19
- Target: `main`
- Base: `ee4305a`
- Merge base: `ee4305a`
- Head: `6f82d26`
- Branch: `docs/pr-83-skip-set-decision`

## Provider gate

The author provider is Claude Code. Session 64 in `docs/session-handoff.md` names Claude Code as the author of PR #19. The reviewer provider is Codex. The providers differ, so the gate passes under T-4 and D-17.

## Intended behavior and scope

PR-83 records the docs-only skip paths and the transitional prompt rule. It updates the CI comment, the CI roadmap, D-600 and D-601, both agent files, and the one-PR skill. D-602 adds a review procedure for a session with no network. The review inspected all 11 paths in the full PR diff: `.claude/skills/one-pr-one-session/SKILL.md`, `.claude/skills/pr-review/SKILL.md`, `.github/workflows/ci.yml`, `AGENTS.md`, `CLAUDE.md`, `docs/decisions.md`, `docs/reviews/pr-19-response.md`, `docs/reviews/pr-19.md`, `docs/roadmaps/area-ci.md`, `docs/session-handoff-archive.md`, and `docs/session-handoff.md`.

Since the prior effective head `38aa19f`, the diff changes five paths: `.claude/skills/one-pr-one-session/SKILL.md`, `docs/reviews/pr-19-response.md`, `docs/reviews/pr-19.md`, `docs/session-handoff-archive.md`, and `docs/session-handoff.md`. The review checked the correction against D-601, the path rules against D-600, the equal agent files against D-20, the no-network review procedure against D-602, and the documents against D-577 and D-589. The PR head is `b862cfb`; the effective head is `6f82d26` because the later commit changes only session handoff metadata.

## Findings

### P2-1: The merged-PR stop rule blocks the transitional prompt

Status: fixed in `6f82d26`.

File: `.claude/skills/one-pr-one-session/SKILL.md:36`.

Trigger: The owner merges the bound PR and says `Merged PR #x`.

Expected: The session writes the transitional prompt for that PR, as D-601 requires.

Actual: Step 1 says to stop when the conversation holds a merged PR. Step 6 then requires the same session to write a prompt after that merge. The stop rule has no exception for that case.

Consequence: The session can stop before it writes the prompt that D-601 requires.

Correction: Add a narrow exception for the bound PR and the exact merge message, while keeping the stop rule for other merged PRs.

Regression check: Follow the written steps for a merged bound PR and the exact merge message. The instructions must permit the prompt and still block work for another merged PR.

Closure evidence: Step 1 now makes the bound PR's transitional prompt the one exception. It still blocks a merge message for another PR. The fix matches both cases in the regression check.

## Out of scope

None.

## PR comments

No review threads exist. The issue comments include two owner `Gitar review` requests, two Gitar acknowledgments, and two dashboard comments. The latest dashboard approves the correction at `6f82d26` with no finding. The approval is stale for current PR tip `b862cfb`, which followed the dashboard update.

## Description edits

The previous round corrected the stale handoff line. Old: `docs/session-handoff.md` listed Session 64 as the top entry and Session 54 as moved to the archive. New: the line names both handoff files, Sessions 64 to 71, and Session 60 in the archive. This round changed the session range from 64 to 71 to 64 to 72.

## Verification

- `git fetch origin`: passed. `origin/main` is `ee4305a`, and the PR head is `b862cfb`.
- `git diff --stat ee4305a...6f82d26`: passed; 11 paths listed above. `git diff --stat 38aa19f...6f82d26`: passed; 5 paths listed above. Both `git diff --check` commands passed.
- The correction in `6f82d26` moves the exception into the merged-PR stop condition and limits it to the bound PR. The original trigger now permits the transitional prompt and blocks a different PR's merge message.
- `make verify`: passed. Build succeeded with 0 warnings and 0 errors; all 8 non-smoke tests passed; format, STE, and the Godot smoke session passed. `det-lint` does not exist yet; PR-46 creates it (G-16).
- `cmp -s CLAUDE.md AGENTS.md`: passed; files are identical.
- `gh pr checks 19`: all 9 live checks pass on current PR tip `b862cfb`. `review-gate` does not exist yet (PR-3 creates it).
- PR comment and description export: passed. The PR is open, targets `main`, and has base `ee4305a`. The latest Gitar dashboard approves `6f82d26` with no finding.
- `gh api graphql` review thread query: passed; 0 review threads. The review API returns no submitted review.
- Gitar check run: passed on `6f82d26` at `13:01:58Z`; its request and dashboard times are `13:01:49Z`, `13:02:11Z`, and `13:02:51Z`. Metadata commit `b862cfb` followed at `13:03:25Z`, and has no Gitar run. The pass is stale for the PR tip.
- `git status --short --branch`: local and remote branches match; unrelated untracked `deck-test/` remains untouched.
- Push: <pending>.

## Open questions and accepted risks

No open OQ affects PR-83. The Gitar review is stale for the current PR head, which blocks the review gate.

## Earlier verdicts

- **Blocked**, Session 65, for head `c065a11`: GitHub state and the local build result were unavailable.
- **Blocked**, Session 67, for head `c065a11`: the review could not verify GitHub evidence or publish its record.
- **Changes required**, Session 70, for head `38aa19f`: P2-1 found a conflict in the merged-PR stop rule.

## Verdict

**Blocked.** This verdict applies to effective head `6f82d26`. Finding P2-1 is fixed, and the local and CI checks pass. The Gitar pass approves `6f82d26`, but the current PR tip `b862cfb` has no matching Gitar run.
