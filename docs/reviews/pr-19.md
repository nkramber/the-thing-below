# PR-19 review

Date: 2026-09-17

## Identity

- PR: 19
- Target: `main`
- Base: `ee4305a`
- Merge base: `ee4305a`
- Head: `7732b1b`
- Branch: `docs/pr-83-skip-set-decision`

## Provider gate

The author provider is Claude Code. Session 64 in `docs/session-handoff.md` names Claude Code as the author of PR #19. The reviewer provider is Codex. The providers differ, so the gate passes under T-4 and D-17.

## Intended behavior and scope

PR-83 records the docs-only skip paths and the transitional prompt rule. It updates the CI comment, the CI roadmap, D-600 and D-601, both agent files, and the one-PR skill. D-602 adds a review procedure for a session with no network. D-603 sets the effective head for a Gitar pass. The review inspected all 12 paths in the full PR diff: `.claude/skills/gitar-review/SKILL.md`, `.claude/skills/one-pr-one-session/SKILL.md`, `.claude/skills/pr-review/SKILL.md`, `.github/workflows/ci.yml`, `AGENTS.md`, `CLAUDE.md`, `docs/decisions.md`, `docs/reviews/pr-19-response.md`, `docs/reviews/pr-19.md`, `docs/roadmaps/area-ci.md`, `docs/session-handoff-archive.md`, and `docs/session-handoff.md`.

Since the prior effective head `6f82d26`, two paths changed: `.claude/skills/gitar-review/SKILL.md` and `docs/decisions.md`. The review checked the Gitar freshness rule against D-603. It also checked the correction against D-601, the path rules against D-600, the equal agent files against D-20, the no-network procedure against D-602, and the documents against D-577 and D-589. PR tip `784ebbf` changes only `docs/session-handoff.md` after effective head `7732b1b`.

## Findings

### P2-1: The merged-PR stop rule blocks the transitional prompt

Status: fixed in `6f82d26`.

File: `.claude/skills/one-pr-one-session/SKILL.md:36`.

Trigger: The owner merges the bound PR and says `Merged PR #x`.

Expected: The session writes the transitional prompt for that PR, as D-601 requires.

Actual: Step 1 says to stop when the conversation holds a merged PR. Step 6 then requires the same session to write a prompt after that merge. The stop rule has no exception for that case.

Consequence: The session can stop before it writes the prompt that D-601 requires.

Correction: Add a narrow exception for the bound PR and its merge message. Keep the stop rule for other merged PRs.

Regression check: Follow the written steps for a merged bound PR and the exact merge message. The instructions must permit the prompt and still block work for another merged PR.

Closure evidence: Step 1 now makes the bound PR's transitional prompt the one exception. It still blocks a merge message for another PR. The fix matches both cases in the regression check.

## Out of scope

None.

## PR comments

No review threads exist. The issue comments include three owner `Gitar review` requests, three Gitar acknowledgments, and three dashboard comments. The latest dashboard approves with no finding. The manual request was at `14:43:56Z`. Gitar replied `On it` at `14:44:17Z`. Dashboard comment `5716314911` was edited at `14:45:14Z`. Its check on `7732b1b` passed from `14:44:01Z` to `14:45:15Z`. The later tip `784ebbf` changes only `docs/session-handoff.md`. D-603 says the pass remains current. The PR has zero review threads.

## Description edits

The previous round corrected the stale handoff line. Old: `docs/session-handoff.md` listed Session 64 as the top entry and Session 54 as moved to the archive. New: the line names both handoff files, Sessions 64 to 71, and Session 60 in the archive. Later edits changed the range from Sessions 64 to 71, to 64 to 72, and then 64 to 73. This review updates it to Sessions 64 to 74 and names Session 63 as moved to the archive.

## Verification

- `git fetch origin`: passed. `origin/main` is `ee4305a`, and the PR head is `784ebbf`.
- `git diff --stat ee4305a...7732b1b`: passed; 12 paths listed above. `git diff --stat 6f82d26...7732b1b`: passed; two paths listed above. `git diff --check ee4305a..7732b1b`: passed.
- The correction in `6f82d26` moves the exception into the merged-PR stop condition and limits it to the bound PR. The original trigger now permits the transitional prompt and blocks a different PR's merge message.
- `make verify` on local tree `784ebbf` plus review metadata: passed. Build succeeded with 0 warnings and 0 errors; all 8 non-smoke tests passed; format, STE, and the Godot smoke session passed. `det-lint` does not exist yet; PR-46 creates it (G-16).
- `cmp -s CLAUDE.md AGENTS.md`: passed; files are identical.
- `gh pr checks 19`: all 9 required live checks pass on PR tip `784ebbf`. `review-gate` does not exist yet (PR-3 creates it).
- PR comment and description export: passed. The PR is open, targets `main`, and has base `ee4305a`. The latest Gitar dashboard approves with no finding.
- `gh api graphql` review thread query: passed; 0 review threads. The review API returns no submitted review.
- The Gitar check on effective head `7732b1b` passed. The request was at `14:43:56Z`; the reply `On it` came at `14:44:17Z`; and the dashboard edit came at `14:45:14Z`. The check ran from `14:44:01Z` to `14:45:15Z`. `git diff --stat 7732b1b..784ebbf` lists only `docs/session-handoff.md`, so D-603 keeps the pass current.
- `git status --short --branch`: local and remote branches match; unrelated untracked `deck-test/` remains untouched.
- Push: `b03e1d21f28d03e03ca72312e518802900eb2b4c` is the head of `origin/docs/pr-83-skip-set-decision`, verified with `gh pr view`.

## Open questions and accepted risks

No open OQ affects PR-83. The `review-gate` check does not exist yet; PR-3 creates it (G-16).

## Earlier verdicts

- **Blocked**, Session 65, for head `c065a11`: GitHub state and the local build result were unavailable.
- **Blocked**, Session 67, for head `c065a11`: the review could not verify GitHub evidence or publish its record.
- **Changes required**, Session 70, for head `38aa19f`: P2-1 found a conflict in the merged-PR stop rule.
- **Blocked**, Session 72, for head `6f82d26`: the review treated a metadata-only tip as making the Gitar pass stale.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `7732b1b`. P2-1 is fixed, D-603 resolves the freshness condition, all required checks pass, and no blocking finding remains.
