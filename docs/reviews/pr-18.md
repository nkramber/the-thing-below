# PR-18 review

Date: 2026-09-16

## Identity

- PR: 18
- Target: `main`
- Base: `9f27f1218c6289db60ca7856018dbbbc22690921`
- Merge base: `9f27f1218c6289db60ca7856018dbbbc22690921`
- Head: `d8c31b8e08b4db24a4286b2917a66d0aa3d2abd8`
- Branch: `feat/pr-1-scaffold`

## Provider gate

Session 57 names Claude Code as the author. The owner assigned this review to Codex. Codex is the opposite provider, so the gate passes under T-4 and D-17.

## Intended behavior and scope

PR-1 creates the solution scaffold, initial Core, Game, Tools, and Tests projects, local commands, hook, CI jobs, and records the Deck test answers. The review inspected the full diff and the changed workflow, project files, tests, tools, Makefile, hook, rules, decisions, questions, design text, roadmaps, and handoff history.

The repeat review inspected the full diff since `0c402dd`: `Makefile`, `.github/workflows/ci.yml`, `docs/design.md`, `docs/reviews/pr-18-response.md`, `docs/roadmaps/area-ci.md`, and `docs/roadmaps/phase-1-foundations.md`. It also read the updated review record and handoff. No changed path remains uninspected.

The second repeat review inspected the full diff since `6e0622a`: `docs/design.md`, `docs/reviews/pr-18-response.md`, and `docs/roadmaps/phase-1-foundations.md`. It also read the current review record, the response file, the latest handoff entry, and the PR description. No changed path remains uninspected.

Inspected paths: `.claude/skills/csharp-conventions/SKILL.md`, `.githooks/pre-commit`, `.github/workflows/ci.yml`, `.gitignore`, `AGENTS.md`, `CLAUDE.md`, `Directory.Build.props`, `Makefile`, `TheThingBelow.Core/CoreAssembly.cs`, `TheThingBelow.Core/TheThingBelow.Core.csproj`, `TheThingBelow.Game/Boot.tscn`, `TheThingBelow.Game/TheThingBelow.Game.csproj`, `TheThingBelow.Game/project.godot`, `TheThingBelow.Game/scripts/Boot.cs`, `TheThingBelow.Game/scripts/Boot.cs.uid`, `TheThingBelow.Tests/AgentFileTests.cs`, `TheThingBelow.Tests/CoreReferenceTests.cs`, `TheThingBelow.Tests/RepositoryRoot.cs`, `TheThingBelow.Tests/TheThingBelow.Tests.csproj`, `TheThingBelow.Tests/ToolsCommandLineTests.cs`, `TheThingBelow.Tools/Program.cs`, `TheThingBelow.Tools/TheThingBelow.Tools.csproj`, `TheThingBelow.slnx`, `docs/decisions.md`, `docs/design.md`, `docs/questions.md`, `docs/roadmaps/area-ci.md`, `docs/roadmaps/area-effects.md`, `docs/roadmaps/area-release.md`, `docs/roadmaps/phase-1-foundations.md`, `docs/roadmaps/phase-2-first-playable.md`, `docs/session-handoff-archive.md`, `docs/session-handoff.md`, and `global.json`.

Affected contracts: Core boundary, dependency decisions, build and test behavior, CI trust and permissions, failure reporting, workflow SHA pins, and repository documentation gates (D-20, D-100, D-217, D-511, D-592 to D-599, G-1, G-13, T-2, T-3, T-4, D-577, D-581, D-560). No gameplay or content contract applies because this PR adds no gameplay or content.

## Findings

### P2-1: The local smoke target can accept a failed Godot build

Status: withdrawn.

File: `Makefile:41-43`.

Trigger: The Godot editor's build callback fails while `--build-solutions` returns exit code 0, as F-60 stated.

Expected: The local smoke check must report a build failure and stop before it can report success (T-2, D-117).

Actual: The trigger is refuted. A direct run after removing the Game project's `net10.0` pin logged `An EditorPlugin build callback failed` and returned exit code 1. The original Makefile ran this command directly, so `make` would fail. The earlier measurement read the status of `tail` through a pipe, not the Godot process.

Consequence: The stated build-failure case did not cause a false pass on `0c402dd`.

Correction: Withdraw this trigger. F-60 now marks the exit-code claim as refuted in part.

Regression check: The direct Godot run on the F-60 trigger returned 1. The corrected `make smoke` target also fails on the build error.

### P2-2: The review document line does not follow the required form

Status: fixed in `6e0622a5e30f5c546eaefa8029bc1924b5f98c15`.

File: PR description, Documents section, `docs/reviews/` line.

Trigger: Read the PR description's Documents section before review publication.

Expected: Each Documents line starts with its row name and uses an allowed form. The pre-review `docs/reviews/` line names the review that the PR awaits (D-577, D-581).

Actual: The corrected line reads `` `docs/reviews/`: Changed: `docs/reviews/pr-18.md` and `docs/reviews/pr-18-response.md`. The review record of this PR, and the answer to it. `` It follows the required form and names the actual files.

Consequence: The Documents line now satisfies the gate.

Correction: Fixed in `6e0622a` by updating the PR description.

Regression check: The current PR description names both actual review files in the `Changed:` form.

### P2-3: The smoke session hangs when its managed assembly fails to load

Status: fixed in `6e0622a5e30f5c546eaefa8029bc1924b5f98c15`.

File: `Makefile:59-64`, `.github/workflows/ci.yml:261-272`.

Trigger: The scene cannot instantiate its managed boot script. The old smoke commands had no frame limit, and the session did not reach `Quit`.

Expected: Each smoke check must end promptly and prove that the boot script reached its success line (T-2, D-117).

Actual: The old local command waited without end. With `--quit-after`, Godot returned 0 but wrote no success line.

Consequence: The local check could wait indefinitely, and the CI job could run until its 30-minute timeout.

Correction: Add `--quit-after 600` and fail if the success line is absent.

Regression check: `make smoke` passed on a healthy tree. A wrapper removed the built Game assembly before the real Godot session. `make smoke` failed on the missing success line and the wrapper restored the assembly.

### P2-4: The review response contradicts its regression table

Status: fixed in `d8c31b8e08b4db24a4286b2917a66d0aa3d2abd8`.

File: `docs/reviews/pr-18-response.md:28,47`.

Trigger: Compare the stated result for the missing-boot-script case with the result in the regression table.

Expected: The response must report one result for the same command and trigger (T-5, D-14).

Actual: The response now states that the old target ran without end for this trigger. It separately explains that a frame limit alone would expose the second fault as a false pass. The regression table and the text after it give the same old and corrected results.

Consequence: The response now describes the two faults and their combined correction without conflicting with its regression table.

Correction: The response names the missing frame limit and log check, then explains why both are needed.

Regression check: The P2-1 explanation, regression table, and concluding paragraph were read together. Each says the old command runs without end, while the corrected target fails when the success line is absent. Fixed in `d8c31b8`.

## Out of scope

None.

## PR comments

- Gitar's current dashboard comment, edited at `2026-09-17T03:04:49Z`, approves `d8c31b8` with one closed finding and no open issues. The finding claimed that the macOS smoke pattern missed `Godot_mono.app`. Commit `0c402dd` fixed that pattern, and all three current smoke legs pass. The response file records one resolved inline thread. The owner confirms that zero inline threads remain unresolved.

## Description edits

None. The author corrected the Documents line in the response round. This review made no description edit.

## Verification

- `git fetch origin`: passed. `origin/main` is `9f27f1218c6289db60ca7856018dbbbc22690921`; the branch tip before this review is `47a7e1639104b69d726c993c4404e4bcbbe552fd`.
- Effective-head check: `d8c31b8e08b4db24a4286b2917a66d0aa3d2abd8` is the latest commit that changes a non-metadata path. `47a7e1639104b69d726c993c4404e4bcbbe552fd` changes only `docs/reviews/pr-18.md` and `docs/session-handoff.md` after that head.
- Second repeat diff: `git diff --stat 6e0622a5e30f5c546eaefa8029bc1924b5f98c15..d8c31b8e08b4db24a4286b2917a66d0aa3d2abd8` passed. It changes `docs/design.md`, `docs/reviews/pr-18-response.md`, and `docs/roadmaps/phase-1-foundations.md`, plus metadata. Each path was read in full.
- `git diff --check 9f27f1218c6289db60ca7856018dbbbc22690921..d8c31b8e08b4db24a4286b2917a66d0aa3d2abd8`: passed.
- P2-4 verification: passed. The response explanation and regression table both state that the old unbounded target runs without end. Both also state that a frame limit alone would make the missing success line a false pass. The Makefile and CI check both the frame limit and success line.
- `gh pr view 18 --json headRefOid,baseRefOid,headRefName,baseRefName`: passed. PR #18 is open, targets `main` at `9f27f1218c6289db60ca7856018dbbbc22690921`, and has branch tip `47a7e1639104b69d726c993c4404e4bcbbe552fd`.
- Current CI checks on branch tip `47a7e1639104b69d726c993c4404e4bcbbe552fd`: all nine pass: changed paths, STE check, build/test/format on Ubuntu, Windows, and macOS, coverage, and smoke on all three platforms.
- Current Gitar dashboard comment: `2026-09-17T03:04:49Z`, after the `d8c31b8` push and the `On it` reply. It approves the head with one closed finding and no open issues.
- Inline comment export: the response file records the exact GraphQL query result as zero unresolved threads. A fresh run in this review failed with `error connecting to api.github.com`. The owner confirmed that zero inline threads remain unresolved and directed approval on that basis.
- `make verify`: not run in this review. The current nine CI checks pass, and the focused P2-4 check read the response, design row, and roadmap row.
- `git diff --stat 9f27f1218c6289db60ca7856018dbbbc22690921...d8c31b8e08b4db24a4286b2917a66d0aa3d2abd8`: passed; all 36 paths in the PR stat were inspected in the earlier review rounds. The new diff paths listed above were inspected again.
- Repeat diff: `git diff --stat 0c402dd6611fdb49de79b604d7bf012e76557a40...6e0622a5e30f5c546eaefa8029bc1924b5f98c15` passed; the correction changes the smoke commands and their supporting records.
- `git diff --check 9f27f1218c6289db60ca7856018dbbbc22690921..6e0622a5e30f5c546eaefa8029bc1924b5f98c15`: passed.
- F-60 trigger reproduction: passed as a refutation. After the temporary removal of the Game target-framework pin, Godot logged the callback failure and returned 1. The project file was restored, and `git diff` reports no change.
- `make smoke`: passed on the healthy tree under the owner machine context.
- Missing-assembly regression: passed. A wrapper moved the built assembly during the real Godot session; `make smoke` failed on the absent success line and the wrapper restored the assembly.
- `gh pr view 18`: passed earlier in the session. PR is open, base is `main`, tip is `a56a7ce79cce384f4492e32288166f5f507f889e`; all nine CI checks passed on this tip.
- Current Gitar summary: approval, one closed finding, no open issues. The author handoff records the review and reply times after the push of `6e0622a`.
- Full comment export: incomplete. `gh api` and GraphQL thread requests failed to connect. The current dashboard summary and response file cover the known Gitar finding, but do not independently enumerate inline threads.
- `make verify`: not run. CI passed all required checks, and the focused smoke checks ran locally.
- Push: `47a7e1639104b69d726c993c4404e4bcbbe552fd` is the head of `origin/feat/pr-1-scaffold`, verified with `gh pr view`.
- `git diff --stat 9f27f1218c6289db60ca7856018dbbbc22690921...0c402dd6611fdb49de79b604d7bf012e76557a40`: passed; 34 changed paths inspected.
- `git diff --check 9f27f1218c6289db60ca7856018dbbbc22690921...0c402dd6611fdb49de79b604d7bf012e76557a40`: passed.
- `cmp -s AGENTS.md CLAUDE.md`: passed; files match.
- `gh pr view 18 --json ...`: passed. PR is open, base is `main`, and tip is `a9a3d0724bab8d226e45440637ccca2380692ef9`.
- CI run 35169864293 on tip `b32a97139a2cc604bd2fc5048caca0c0c792cd0a`: all nine checks passed, including build, test, format, coverage, smoke on all three legs, and STE check.
- CI run 35171555340 on tip `a9a3d0724bab8d226e45440637ccca2380692ef9`: all nine checks passed, including build, test, format, coverage, smoke on all three legs, and STE check.
- `make verify`: not completed. The local `dotnet build` produced no output for 60 seconds and was interrupted. Local test, format, STE, and smoke commands did not run.
- Required GitHub comment export: incomplete. The issue comment and review summaries came from `gh pr view`, but the API calls for inline threads failed to connect.
- Push: `a9a3d0724bab8d226e45440637ccca2380692ef9` is the head of `origin/feat/pr-1-scaffold`, verified with `gh pr view`.

## Open questions and accepted risks

No open OQ affects PR-1. The review accepts no risk.

## Earlier verdicts

**Blocked** for head `0c402dd6611fdb49de79b604d7bf012e76557a40`: P2-1 and P2-2 were open, and the comment export was incomplete.

**Blocked** for head `6e0622a5e30f5c546eaefa8029bc1924b5f98c15`: P2-4 remained open because the response contradicted its regression table, and the inline comment export was incomplete.

## Verdict

**Ready for owner merge.** This verdict applies to head `d8c31b8e08b4db24a4286b2917a66d0aa3d2abd8`. The earlier findings are fixed, all nine current CI checks pass, Gitar approves the head, and the owner confirms that zero inline threads remain unresolved.
