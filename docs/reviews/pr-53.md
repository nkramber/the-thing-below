# PR-53 review

Date: 2026-09-21

## Identity

- PR: 53
- Target: `main`
- Base: `e0cc485`
- Merge base: `e0cc485`
- Head: `30868a8`
- Branch: `feat/pr-56-light-and-shadows`

## Provider gate

The handoff entries for PR #53 identify Claude Code as the author provider. The active reviewer is Codex. The providers differ, so the gate passes T-4 and D-17.

## Intended behavior and scope

PR-56 adds deterministic light content, map decor, ambient and point lights, wall shadows, normal-map atlas pages, a carried light, a battle key light, effect limits, and their tests. The review inspected the complete diff from `e0cc485` through `6095f70`, the PR description, the existing comment, the affected callers, content validation, presentation wiring, and the roadmap exit tests.

Inspected paths from `git diff --stat`: `.claude/skills/ste-writing/references/glossary.md`; all changed files under `TheThingBelow.Core/`, `TheThingBelow.Debug/`, `TheThingBelow.Game/`, and `TheThingBelow.Tests/`; all changed files under `content/`; `docs/decisions.md`; `docs/design.md`; `docs/questions.md`; the three changed roadmap files; both handoff files; all changed screen baselines; and the changed identity file.

The affected contracts are content validation, deterministic Core boundaries, replay identity, error context, presentation lighting, normal maps, effect limits, screen captures, and the Documents section. No affected area remains uninspected.

## Earlier verdicts

- Head `6095f70`: **Ready for owner merge.** The initial review found no actionable defect.

## Findings

No finding.

## Out of scope

The torch item state and the later HD-2D passes belong to PR-91 and PR-92, as the roadmap records. They are not defects of PR-53.

## PR comments

- Gitar reported one closed comment about the `FeetShadow` shape name and approved the current code. The review verified that `f4b071b` changes the comment from octagon to hexagon. Functional validation was not enabled, so it does not replace the independent review or the local smoke and screen evidence.

## Description edits

None.

## Verification

- `git fetch origin`: passed. The branch head is `30868a8`; the metadata tip is `b197a02` before this review commit.
- `git diff --stat 6095f70...30868a8`: inspected the complete correction diff, including wall geometry, figure shadows, paired lights, budget changes, baselines, and the Deck evidence.
- `make verify` at `30868a8`: passed. Build, 1,862 non-smoke tests, format, det-lint, STE, replay identity, content hash, atlas check, and smoke passed. Smoke reported 20 lights, 214 occluders, the carried-light switch, and one battle key light.
- Boundary regression: `WallShadowsTests` passed for south and east faces, a one-tile wall strip, a corner wall, a wall mass, and a full blocking row. `LightContentTests` passed the paired-light budget and source-count boundary.
- `gh pr view 53`: the current CI run passes build/test/format on Ubuntu, Windows, and macOS, det-lint, replay identity on all three legs, smoke on all three legs, screen-test, STE, and Gitar. Review-gate waits for this updated record.
- Deck evidence: the PR records `pairs-24` at 4.17 ms and `full-load-24` at 4.55 ms at the 95th percentile under Mobile, with no frame over 16.667 ms.
- Push: `<pending>` is the head of origin/feat/pr-56-light-and-shadows, verified with gh pr view.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** This verdict applies to head `30868a8`. The earlier findings are fixed, the adjacent boundary checks pass, the complete diff has review coverage, and no blocking finding remains.
