# PR-53 review

Date: 2026-09-21

## Identity

- PR: 53
- Target: `main`
- Base: `e0cc485`
- Merge base: `e0cc485`
- Head: `6095f70`
- Branch: `feat/pr-56-light-and-shadows`

## Provider gate

The handoff entries for PR #53 identify Claude Code as the author provider. The active reviewer is Codex. The providers differ, so the gate passes T-4 and D-17.

## Intended behavior and scope

PR-56 adds deterministic light content, map decor, ambient and point lights, wall shadows, normal-map atlas pages, a carried light, a battle key light, effect limits, and their tests. The review inspected the complete diff from `e0cc485` through `6095f70`, the PR description, the existing comment, the affected callers, content validation, presentation wiring, and the roadmap exit tests.

Inspected paths from `git diff --stat`: `.claude/skills/ste-writing/references/glossary.md`; all changed files under `TheThingBelow.Core/`, `TheThingBelow.Debug/`, `TheThingBelow.Game/`, and `TheThingBelow.Tests/`; all changed files under `content/`; `docs/decisions.md`; `docs/design.md`; `docs/questions.md`; the three changed roadmap files; both handoff files; all changed screen baselines; and the changed identity file.

The affected contracts are content validation, deterministic Core boundaries, replay identity, error context, presentation lighting, normal maps, effect limits, screen captures, and the Documents section. No affected area remains uninspected.

## Findings

No finding.

## Out of scope

The torch item state and the later HD-2D passes belong to PR-91 and PR-92, as the roadmap records. They are not defects of PR-53.

## PR comments

- Gitar reported one review-gate tool failure and approved the code with no issue. The review verified that the failure is the expected missing review record at `6095f70`, while the current CI run passes the implementation checks. Gitar functional validation was not enabled, so it does not replace the independent review or the local smoke and screen evidence.

## Description edits

None.

## Verification

- `git fetch origin`: passed. The local branch tracks `origin/feat/pr-56-light-and-shadows` at `6095f70`.
- `make verify` at `6095f70`: passed. Build, 1,856 non-smoke tests, format, det-lint, STE, replay identity, content hash, atlas check, and smoke passed. Smoke output recorded the map lights, carried-light switch, and one battle key light.
- `gh pr view 53`: the current CI run passes build/test/format on Ubuntu, Windows, and macOS, det-lint, replay identity on all three legs, smoke on all three legs, screen-test, STE, and Gitar. The only failure is review-gate, which waits for this record.
- Screen review: inspected the changed map and battle 1x baselines. The map shows ambient darkness, three torch pools, hard wall shadows, and the party. The battle shows the key light and readable combatants. The PR records the author's `make sheet` review of map, walk, and battle frames.
- Push: `<pending>` is the head of origin/feat/pr-56-light-and-shadows, verified with gh pr view.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** This verdict applies to head `6095f70`. The opposite-provider gate passes, the complete diff has review coverage, the required local and CI evidence passes, and no blocking finding remains.
